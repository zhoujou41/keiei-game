# 経営ゲーム プロトタイプ 進捗メモ

最終更新: 2026-09-21（Claude作業ログ・Phase 5ステージ7完了時点）

このファイルは「今どこまで出来ていて、何を確認済みで、次に何をすべきか」を記録するための
進捗ドキュメントです。作業を別セッション（別のClaude、あるいは人間）が引き継ぐ場合も、
まずこのファイルを読めば状況が分かるようにしています。

**Phase 1**: 経営シミュレーションの中核ループ（仕入れ・シフト・マネジメント・スキル・
経営目標・テキストログ演出）を実装。
**Phase 2**: 「店の中・厨房の動きが見えない」というフィードバックを受けて、店内レイアウト編集・
パスファインディング・キャラクターの見た目生成・実際に人が歩いて料理を運ぶ/客が着席して
食事する空間アニメーションを追加。
**Phase 3（今回追加分）**: 「3等身デフォルメのキャラクターが店内で小さな芝居をしている」見た目
に近づけるため、キャラクターの頭身比率・表情差分・歩行/食事/配膳アニメーションを作り込み、
店内什器（テーブル・チェア・厨房設備・レジ・入口）をイラスト風の複合図形に刷新。詳細は
第4-c節。
**Phase 4（今回追加分）**: 「店舗の様子が全然どうなっているか分からない」というフィードバックを
受けて、テキストログと空間アニメーションが別々の仕組みで独立に再生されていた構造を、
1行のテキスト＝1つの演出アクションとして必ずセットで進行する「beat」という単一のデータ列に
統合。さらに曜日タブで任意の曜日だけを選んで見返せるようにし、ログ表示を全文スクロールから
直近3行のローリング表示に変更した。詳細は第4-d節。
**Phase 5・ステージ1（今回追加分）**: ユーザー提供の `chibi_restaurant_assets.zip`（実PNG素材）を
正式な素材として採用し、SVGプログラム生成のキャラクター表示から、PNGレイヤー合成方式へ移行を
開始。まず「客1人・スタッフ1人・テーブル1・椅子1・厨房1・料理1種」の最小構成で、
Customer/Staffそれぞれの状態遷移（FSM）に基づく自律的な入店〜退店の1ループを新エンジンとして
独立に実装・動作確認した（既存のOFFICE/LAYOUT/FIELDの本編フローはまだ変更していない）。
詳細は第4-e節。
**Phase 5・ステージ2（今回追加分）**: 「動きがもっさりしている、高速化して次へ進めて」という
フィードバックを受けて全体のテンポを大幅に引き上げ、続けて客1人・スタッフ1人の固定構成から
「テーブル3卓・客が時間差でランダムに来店し最大6人まで同時進行、スタッフの手が足りないと
実際に混雑して諦めて帰る客が出る」ボトルネック可視化の構成に拡張した。さらにユーザーから
新しく提供された`chibi_restaurant_assets_v2.zip`を正式素材として採用し、キャラクター素材一式を
差し替えた。詳細は第4-f節。
**Phase 5・ステージ3〜4（既存分）**: LimeZu「Modern Interiors」への素材全面差し替え、
コック二重表示バグ修正・多フレームアニメ化・軸移動化・什器実素材化。詳細は第4-g節・4-h節。
**Phase 5・ステージ5（今回追加分）**: 「テーブルを縦長に」「椅子は左右2脚ずつ計4脚」
「料理をテーブルに置く」「キッチンを広くしてカウンター経由でウェイターが受け取る」
「ウェイターは黒服の人でよい」「レジを入口近くに置き会計金額をポップアップさせる」という
6点のフィードバックを受けて、テーブルを縦長の実素材に差し替えて1卓4席（左右2脚ずつ）に
拡張し、実際に1卓最大4人まで同時着席できるようにした。あわせてスタッフを「コック」
（コンロ専任、客席には出ない）と「ウェイター」（接客・カウンターでの受け取り・配膳・会計）の
2人体制に分離し、厨房の注文キュー／カウンターの受け取り待ちキューを介して協調させた。
料理は客について回るのではなく席ごとの卓上固定点に置かれるようにし、レジを入口近くに
配置して会計金額がレジから飛び出すポップアップ演出を追加した。詳細は第4-i節。
**Phase 5・ステージ6（今回追加分）**: 「卓の右の椅子が反転していない」「キッチンをもっと
細かく・広く」「客が何をしているか分からない・急に消えたように見える」「日付/時刻・
入店数・所持金を表示したい」「レジの金額と合計所持金を連動させたい」という5点の
フィードバックを受けて、卓の右側の椅子を左右反転して両側とも卓を向くようにし、
キッチンに電子レンジ・食洗機・パントリー棚を追加して什器点数を3→6に増やしつつ
キャンバス幅を拡張してキッチンの占有面積も広げた。客の状態（席を探している／注文待ち／
料理待ち／食事中／会計中／退店中）ごとに頭上アイコンを常時表示するようにし、ゲーム内の
日付・時刻・本日/週間入店数・所持金を管理する簡易クロックを新設して検証ページ右側の
HUDパネルに表示、会計ポップアップの金額をその瞬間に所持金へも加算するようにした。
詳細は第4-j節。
**Phase 5・ステージ7（今回追加分）**: 「ゲームに反映して」の指示を受けて、ステージ1〜6は
`asset_test.html`という単体検証ページの中だけの機能だった新PNGエンジンを、本編
`index.html`のFIELD画面に統合した。ユーザーに事前確認した通り、`core/simulation.js`の
数値計算・beat生成ロジックは一切変更せず「見た目だけ」を差し替え、かつLAYOUT画面で
プレイヤーが自由編集した配置（`Game.App.state.layout`）が新エンジンの描画にそのまま
反映されるようにした（固定レイアウトのハードコードは行っていない）。あわせて、
旧`scene.js`では「コックが自分で調理して自分で客席まで運ぶ」1人二役になっていた
厨房フローを、「コックが調理してカウンター地点に置く→ウェイター役のスタッフが
取りに来て客席まで運ぶ」という本来のご要望通りの2段階受け渡しに直し、スタッフ人数も
固定1+1ではなく`Game.App.state.staff`の実際の雇用状況（調理ロール＝コック人数、
接客/レジロール＝ウェイター人数）に対応するようにした。詳細は第4-k節。

---

## 1. 現在のステータス：**動くプロトタイプが完成・動作確認済み（Phase 6「開業準備＋未来視システム」まで反映）**

- `index.html` をブラウザで開くだけで遊べます（ビルド不要・インターネット接続不要）。
- Playwright（自動ブラウザテスト）で以下を確認済みです。
  - 初期画面（OFFICE）に商品・スタッフ・スキルが正しく描画される
  - 仕入れ量ボタン／価格調整ボタンが動作する
  - スキルポイントを消費してスキルレベルを上げられる
  - スキルに「集中」するとヒントテキストが変化する（未来見通しLv1で「来週は◯◯の需要が高そう」等）
  - 「営業開始」→FIELD画面に自動遷移→ログが再生される→結果パネルが表示される
  - 結果パネルの「次の週の準備へ」でOFFICEに戻り、週が進む
  - 資金がマイナスになっても即ゲームオーバーにはならず「借金状態」になり、利息が発生する
  - 借金が一定額を超えるとゲームオーバー画面が出て、「最初からやり直す」で完全リセットできる
  - ブラウザを閉じて開き直しても localStorage 経由で続きから再開できる（セーブ／ロード）
  - 30週連続の自動プレイでもJSエラーが発生しない（コンソールエラーゼロ）
  - **（Phase 2追加）** LAYOUT画面で道具（コンロ／仕込み台／冷蔵庫／レジ／テーブル2〜4人用／
    観葉植物／棚）を配置・撤去・初期化でき、その変更がFIELD画面のアニメーションに反映される
  - **（Phase 2追加）** FIELD画面で「客が入口から入店→グループで着席→注文→スタッフが厨房で
    調理→お盆を持って客の席まで歩いて配膳→客が食事→レジへ寄って退店」という一連の動きが
    実際にキャラクターが歩いて再生される（テレポートではない）
  - **（Phase 2追加）** レイアウトのテーブル・厨房・レジを全部消去した極端な状態でも
    クラッシュしない（入口フォールバックで動作継続）ことを確認済み
  - **（Phase 2追加）** 15週連続プレイ＋リスタートを繰り返してもDOM上のキャラクター数が
    毎回ちゃんと0近辺に戻る（メモリリーク無し）ことを確認済み
  - **（Phase 3追加）** キャラクターが3等身デフォルメ（頭が大きく丸い）の見た目になり、
    歩行時に身体が上下に揺れる・足が交互に動く・進行方向を向く、着席・食事（口の開閉＋
    腕の動き）・トレイを持って運ぶ、といったアニメーションが自然に切り替わることを確認済み
  - **（Phase 3追加）** 状態に応じて表情が変わる（待つ＝退屈そう／食事＝満足そう／
    待ちきれず帰る・入店できず帰る＝怒った顔＋早歩き）ことを確認済み
  - **（Phase 3追加）** 店内什器（テーブル＋チェア＋皿、コンロ＋バーナー、冷蔵庫、レジ、
    入口ドア＋OPENマット、観葉植物、棚）がイラスト風の複合図形になり、木目調の床・奥の壁も
    追加。厨房は調理中だけ湯気が上がる演出つき
  - **（Phase 4追加）** FIELD画面に「準備／月／火／水／木／金／土／日／結果」の曜日タブが
    でき、タブをクリックすると即座にその曜日の営業ログ・店内アニメーションだけを（他の曜日を
    経由せず）見返せる。1日分の再生が終わると自動的に一時停止し、「▶ 次の日へ進む」ボタンで
    次の曜日に進む（曜日をまたいで客が居残ることはなく、日が変わるたびに客だけがリセットされる）
  - **（Phase 4追加）** 営業ログが「◯名が来店し、テーブルに着いた」のようなテキスト1行を
    表示するのと完全に同じタイミングで、店内アニメーション側でも実際にその人数分のキャラクターが
    入店・着席する（テキストとキャラクターの動きが別々の仕組みで非同期に動くことがなくなった）
  - **（Phase 4追加）** 営業ログの表示を全文スクロールから直近3行のみのローリング表示に変更し、
    「今どの出来事が起きているか」が一目で追えるようにした
  - **（Phase 6追加）** 初回起動時に「①難易度（最初の未来視）→②業種→③店名→④初期メニュー」の
    4ステップのセットアップ画面が表示され、回答に応じてOFFICE画面の商品・所持金・HUDバッジが
    変わることを確認済み（詳細は4-l節）
  - **（Phase 6追加）** OFFICE画面とFIELD画面（各曜日タブ）に「🔮 日々の未来視」パネルが表示され、
    両者の内容が一致すること（週の始めに1回だけ生成され、後から再生成されない）を確認済み
  - **（Phase 6追加）** 旧セーブ（このセットアップ機能より前に保存されたデータ）を読み込んでも
    セットアップ画面を経由せず、そのまま続きから遊べることを確認済み（自動的にNORMAL難易度・
    仮の店名で補完される）

## 2. 遊び方（プレイヤー向け）

0. 初回起動時は、まずセットアップ画面が表示される。「最初の未来視（難易度：EASY/NORMAL/HARD）」
   →「お店を選ぶ（ラーメン/和食/洋食/デザート）」→「店名を決める」→「初期メニューを選ぶ
   （選んだ業種の中から1品以上）」の4ステップに答えると開業し、OFFICE画面が表示される
   （2回目以降の起動は、続きのセーブがあればこのセットアップ画面は出ない）。
1. `index.html` をダブルクリックしてブラウザで開く（Chrome推奨。file:// で直接開いても、
   簡易サーバー経由でもどちらでも動きます）。
2. OFFICE画面で「🔮今週の未来視（月〜日の抽象的な傾向ヒント）」を確認しつつ、
   「仕入れ」「シフト」「マネジメント方針」「スキル」を設定する。
3. （任意）LAYOUT画面で厨房道具・テーブル・小物の配置を編集する。何もしなければ初期
   レイアウトのまま営業できる。
4. 下部の「この設定で営業開始」を押すとFIELD画面に切り替わり、その週に店で実際に起きた
   出来事が、店内の見取り図の上でキャラクターが歩き回る形＋テキストログの両方で再生される
   （▶再生／⏩倍速／⏭最後まで見る、で速度調整可）。
5. 結果パネルで売上・利益・客数・評判変化・所持金を確認し、「次の週の準備へ」でOFFICEに戻る。
6. 数週間ごとに経営目標（利益・評判・客数）の達成判定があり、未達成でも即終了はしないが
   経営が苦しくなる（評判低下・利息付きの借金・仕入れ条件悪化など）。
7. 所持金が -100,000円を下回るとゲームオーバー。「最初からやり直す」で再スタート。

## 3. ディレクトリ構成

```
スキル経営ゲーム/
├── index.html              画面構成（OFFICE / LAYOUT / FIELD の3画面 + HUD）
├── asset_test.html         【Phase5】新PNGエンジン単体の検証ページ（本編とは独立。日付/時刻/
│                           入店数/所持金を表示するHUDパネル付き）
├── demo_v5.gif             【Phase5・ステージ6】asset_test.html単体検証ページのデモGIF
│                           （キッチン作り込み・客の状態アイコン・HUD表示）
├── demo_v6.gif             【Phase5・ステージ7】★最新デモGIF。本編index.htmlのFIELD画面を
│                           実際にプレイした様子（新PNGエンジン統合後）
├── PROGRESS.md              このファイル
├── assets/                 【Phase5】chibi_restaurant_assets.zip由来のPNG本体（characters/furniture/food/ingredients/ui）
├── css/
│   └── style.css            見た目（仮UI・ダーク基調・スマホ幅対応のレスポンシブ）
└── js/
    ├── core/random.js       乱数ユーティリティ
    ├── core/actorFSM.js     【Phase5・ステージ5】★ Customer/Cook/Waiterの状態遷移（FSM）。画像・DOMを一切知らない純粋ロジック
    ├── data/difficulty.js   【Phase6・新規】★ 難易度＝「最初の未来視」（EASY/NORMAL/HARD）の
    │                        定義と、週次シミュレーションに効かせる倍率。業種による有利不利は無し
    ├── data/storeTypes.js   【Phase6・新規】★ 業種（ラーメン/和食/洋食/デザート）の定義。
    │                        core/simulation.jsのCATEGORIES（中華/和食/洋食/デザート）と対応
    ├── data/products.js     【Phase6で拡張】商品カタログ。業種ごとに3品ずつ計12品
    │                        （Game.Data.PRODUCT_CATALOG）＋後方互換のフラット配列（PRODUCTS）
    ├── data/staff.js        初期スタッフ4人・役割定義
    ├── data/skills.js       スキル10種の定義・レベルアップコスト・集中コスト
    ├── data/events.js       ランダムイベントの定義プール（天候・トレンド・競合など）
    ├── data/layoutObjects.js  【Phase2】配置可能な道具の定義・初期レイアウト
    ├── data/characterParts.js 【Phase2、Phase5・ステージ7で本編から切り離し】旧SVGエンジン用のパーツ定義。
    │                        index.htmlからは読み込まなくなったが、削除はせず保管している（未使用）
    ├── data/assetManifest.js 【Phase5】★ PNG素材（characterAssets/furnitureAssets/foodAssets/animationDefinitions）
    │                        の一元管理。ステージ7でscene.js（本編）からも参照するようになった
    ├── core/economy.js      仕入れコスト・人件費・価格弾力性の計算
    ├── core/goals.js        経営目標（マイルストーン）の生成・判定・ペナルティ
    ├── core/simulation.js   ★ 1週間の営業シミュレーション本体（一番重要なファイル・数値計算）。
    │                        ステージ7の描画エンジン差し替えでは一切変更していない。
    │                        【Phase6で拡張】難易度倍率の合成・曜日別客足のばらつき（dayTraffic）・
    │                        「日々の未来視」テキスト生成（getDailyForesight）を追加
    ├── core/hiring.js       新規雇用（特殊行動）候補生成・雇用コスト・情報開示
    ├── core/pathfind.js     【Phase2】グリッド上のBFS経路探索。ステージ7でも変わらず使用中
    ├── core/layout.js       【Phase2】レイアウトの読み取りヘルパー（通行可否・テーブル一覧等）。
    │                        ステージ7でも変わらず使用中（LAYOUT画面の配置がFIELDの新描画に
    │                        そのまま反映される仕組みの核）
    ├── core/state.js        ゲーム状態の初期化・localStorageへの保存/読込・旧セーブの移行。
    │                        【Phase6で拡張】createInitialState(setupAnswers)がセットアップ画面の
    │                        回答（難易度/業種/店名/初期メニュー）を受け取れるようになった
    ├── ui/setup.js          【Phase6・新規】★ 開業前のセットアップ画面（4ステップウィザード）
    ├── ui/sprite.js         【Phase2、Phase5・ステージ7で本編から切り離し】旧SVGキャラクター描画。
    │                        index.htmlからは読み込まなくなったが、削除はせず保管している（未使用）
    ├── ui/actorSprite.js    【Phase5】★ PNGレイヤー合成の描画エンジン。ステージ7でscene.js（本編）
    │                        からも参照するようになった
    ├── ui/office.js         OFFICE画面のDOM描画とイベント処理
    ├── ui/layoutEditor.js   【Phase2】LAYOUT画面（道具パレット＋グリッド編集）のDOM描画
    ├── ui/scene.js          【Phase2/4、Phase5・ステージ7で全面差し替え】★ FIELD画面の空間アニメーション
    │                        本体。「beatを受け取ってその場で実行するだけ」の受動的エンジンという
    │                        構造・grid/BFSベースの任意レイアウト対応は維持したまま、描画だけを
    │                        actorSprite.js／assetManifest.jsのPNG素材ベースに全面差し替えた
    ├── ui/sceneV2.js        【Phase5】新PNGエンジンの単体検証用「世界」（配置・移動・誰が誰を
    │                        担当するか）。asset_test.htmlからのみ呼ばれる独立モジュール
    │                        （本編スタッフ数固定・レイアウト固定という単体検証専用の簡略化を
    │                        保持したまま残している。本編はscene.js側に個別実装した）
    ├── ui/field.js          【Phase4で全面刷新】FIELD画面の「ディレクター役」。beatを曜日
    │                        ごとにグループ化し、1行ずつテキスト表示とScene.playBeat()呼び出しを
    │                        同時に進める。曜日タブ・3行ログ・結果パネルもここで描画
    └── main.js              アプリ全体の初期化・画面切り替え（OFFICE/LAYOUT/FIELD）・配線
```

設計方針として、**スタッフ／商品／スキル／イベント／経済計算／画面表示** をなるべく別ファイルに
分離しています。例えば商品を増やしたい場合は `js/data/products.js` に1エントリ追加するだけで、
シミュレーション側のロジックは変更不要です（スタッフ・スキルも同様）。

Phase 2でもこの方針を踏襲し、**数値計算（simulation.js）とビジュアル演出（scene.js）を
完全に分離**しています（詳細は4-b節）。

script タグは通常の `<script src="...">` で、ESモジュールは使っていません
（Chromeは `file://` で開いたときESモジュールをCORSでブロックするため、ダブルクリックだけで
動くようにこの方式にしています）。

## 4. 実装済みの機能（今回のプロトタイプ範囲）

- **仕入れ**: 商品ごとに 80/100/110/130% の4段階。仕入れすぎると廃棄、仕入れ不足だと
  売り切れ→機会損失（在庫は翌週へ持ち越さない、との仕様どおり）。
- **シフト**: スタッフ4人、役割（調理／接客／レジ／その他）と勤務日数を個別設定。
  基本勤務日数を超えると1.5倍の割増賃金。
- **マネジメント方針**: 接客重視／調理重視／やる気回復の3配分（合計100に自動調整）。
  配分に応じてスタッフの能力成長・やる気回復速度が変わる。
- **スキル**: 10種類実装。
  - アクティブ（未来見通し／百眼）: 毎週「集中」を選ぶ必要があり、集中するとお金がかかり
    マネジメント効果が下がる（＝強い情報ほど代償が大きい設計）。レベルが上がるほど開示情報が
    「なんとなく」→「具体的な％」に変化。
  - パッシブ（商売／マネージメント／人を見る目／商品開発／個別商品力×4カテゴリ）:
    スキルポイントという有限資源の配分そのものがトレードオフになる。
  - 商品開発スキルは今回「習得はできるが専用UIは未実装」の状態です（次段階での拡張ポイント）。
- **経営目標**: 4週ごとにマイルストーン。利益・評判（・終盤は客数も）の複合目標。未達成でも
  即ゲームオーバーにはならず、評判低下や「借金＋利息」という形でじわじわ苦しくなる。
- **ランダム性**: 天候・SNSトレンド・競合店の動向・仕入れ価格変動・景気など12種類のイベント
  プールから毎週1〜2個が発生。カテゴリ別の需要倍率に影響する。
- **でじこミュニケーション的な「店が動く」演出**:
  FIELD画面で「客が注文→調理→提供」「売り切れ」「行列ができる」「スタッフがサボる」
  「客が待ちきれず帰る」「評判が変化する」といった出来事をテキストログ＋簡易アイコンで
  時系列に再生する。ログの最後に「対応しきれなかった客が多く、評判が下がってしまった」のように
  因果関係が明示される。
- **特殊行動**: 新規雇用のみ実装（候補2名を生成→「人を見る目」スキルレベルに応じて能力の
  開示度が変わる→採用）。値段変更は仕入れパネル内の＋／－ボタンとして常時利用可能。
  メニュー変更・施設増強・スタッフ教育・設備増強はUIにプレースホルダーのみ（未実装）。
- **セーブ／ロード**: ブラウザの localStorage に自動保存。「最初からやり直す」で完全リセット。

## 4-b. Phase 2で追加した「店の中が見える」演出（今回の追加分の詳細）

「再生したときに店の中・厨房の動きが全く見て取れません」というフィードバックを受けて、
以下を実装しました。

### レイアウト編集（LAYOUT画面）
`js/data/layoutObjects.js` + `js/ui/layoutEditor.js`。コンロ／仕込み台／冷蔵庫／レジ／
2人用・4人用テーブル／観葉植物／棚をグリッド上に自由配置・撤去できます。入口セルは固定
（編集不可）。「初期レイアウトに戻す」で標準構成（厨房列＋レジ＋テーブル5卓＋小物3つ）に戻せます。

### 人の動き（パスファインディング）
`js/core/pathfind.js`（グリッドBFS）+ `js/core/layout.js`（どのセルが通行可能か、テーブルの
場所、厨房の作業点、レジの場所などをレイアウトデータから読み取るヘルパー）。配置した道具の
位置と空いている床を元に、キャラクターは実際に「歩いて」目的地まで移動します（テレポート
ではありません）。

### 客の入店〜退店の流れ
`js/ui/scene.js` が、その週の営業シミュレーション結果（`sceneEvents`：来店した客が
「提供された／待ちきれず帰った／店に入らず諦めた」のどれだったかという結果リスト）を元に、
客をグループ単位に再構成し、入口から入店→空いているテーブルに着席（満席なら入店せず
その場で諦めて退店）→注文→厨房のスタッフが調理→料理を客の席まで運んで配膳→客が食事の
モーション→レジに寄って退店、という流れを実際にアニメーションさせます。

### 厨房の動き（料理を運ぶ・配膳する）
厨房スタッフは持ち場に立つ固定キャラクターとして描画され、注文が入るとコンロで調理
（一定時間の「作業中」演出）→お盆（🍜アイコン）を持って客の席まで実際に歩く→テーブルに
配膳して料理アイコンを置く→持ち場に戻る、という一往復の動きを行います。

### キャラクターの見た目 —— ⚠️ ここは依頼内容から仕様変更しています（正直に共有します）

ご依頼は「頭・髪・目・耳・口・鼻・服・ズボン・靴を、それぞれ20パーツ×前後左右4方向の
"画像"として用意する」というものでした。今回のセッションには画像生成ツールが無く、
720枚（20パーツ×4方向×9カテゴリ）の絵を1枚ずつ手描き・用意することは現実的に不可能
だったため、**「あらかじめ用意した図形パーツを、プログラムでレイヤー合成してSVGとして
描画する」方式**に置き換えて実装しました（`js/data/characterParts.js` + `js/ui/sprite.js`）。

具体的には、肌色8種・髪型8種×髪色10種・目6種・口6種・耳4種・服の色12種・ズボンの色10種・
靴の色6種、といった「パーツの形＋色のバリエーション」を持たせ、これを乱数で組み合わせる
ことで、実質何千通りもの見た目の違うキャラクターを生成しています。スタッフは`hashSeed`に
よって同じ人物なら毎回同じ見た目になり（乱数だが再現性あり）、客はランダム生成です。
前後左右4方向の描画にも対応しています（左右は反転で処理）。

「20パーツ×4方向の実写画像」という依頼そのものには応えられていない、という点は
はっきりお伝えしておきたいです。もし本物の画像素材（イラスト発注・画像生成AIサービスとの
連携など）が必要であれば、次のステップとして相談させてください。現状のSVG合成方式でも
「誰が誰だか見分けがつく」「服装や髪色で個性が出る」という体験は実現できていますが、
絵柄としては簡易的なベクター調のキャラクターです。

### 高速再生への配慮
1週間に100人を超える来客があっても再生が長くなりすぎないよう、`MAX_ANIMATED_PARTIES`
（均等間引きサンプリング）と同時アニメーション数の上限、⏩倍速ボタン、即座に結果まで飛べる
「最後まで見る」ボタンを組み合わせています。

### 既知の簡略化（Phase 2分）
- 調理中の演出は「その場で一定時間止まる」だけで、専用の調理モーション（鍋を振る等）はまだ
  ありません。
- レイアウトを全消去した場合、厨房・レジは入口セルにフォールバックして動作を継続します
  （クラッシュはしませんが、見た目としては不自然になります）。
- 空間アニメーションは「見た目の再現」であり、`simulation.js`側の1ティックごとの厳密な
  待ち行列状態と完全に一致しているわけではありません（客のグループ化・間引きは近似です）。
  週ごとの売上・利益などの数値計算自体には一切手を入れていません。

## 4-c. Phase 3で追加した「3等身デフォルメ×自然な芝居」演出（今回の追加分の詳細）

「単純な図形やドラクエ風のキャラクターではなく、3等身程度のデフォルメされた2Dキャラクターが
店内で小さな芝居をしているような見た目にしてほしい」というフィードバックを受けて、
`js/ui/sprite.js`・`js/ui/scene.js`・`js/data/characterParts.js` を中心に作り込みました
（新規ファイルは無く、既存ファイルの中身を刷新する形です）。

### キャラクターの頭身・造形
頭の直径がキャラクター全体の高さの半分近くになるよう調整し（`HEAD_SCALE`定数）、胴体も
角ばった長方形ではなく丸みのある樽型のシルエットに変更しました。髪型も、以前は頭頂部だけを
覆う薄いシルエットだったため（意図せず「後退した生え際」のように見えてしまっていました）、
耳のあたりまでしっかり覆うボリュームのあるシルエットに全8種類描き直しています。頬の赤みを
常時薄く入れることで、3等身キャラクターらしい可愛らしさを出しています。

### 表情の切り替え
状態（`expression`）に応じて目・眉・口の形が変わります。
- 待っている（着席後、料理が来るまで）: 退屈そうな目・口
- 食事中: 口が一口ごとに開閉し、片腕が口元まで往復する
- 満足して会計へ向かう: 笑顔（目が弧を描き、口が大きな笑顔に）
- 待ちきれず帰る／満席で入店できず帰る: 怒った眉・への字口になり、退店時は歩く速さが
  通常の1.5倍になる（＝「怒って急いで帰る」動き）

### 歩行・食事・配膳アニメーション
以前は「2フレームを切り替えるだけ」のアニメーションでしたが、経過時間から連続的な位相
（`walkPhase`）を計算する方式に変更しました。これにより、
- 歩行時: 身体が上下にバウンドする・両足が逆位相で左右に振れる・両腕が振れる・進行方向を
  自動的に向く
- 静止時: ゆっくりした呼吸のような上下動（`idlePhase`）
- 食事: 口の開閉と腕の往復運動が滑らかにループする
- トレイを運ぶ: トレイが軽く揺れながら運ばれる

という、より「人形が生きて動いている」ふうの見た目になっています。また、料理提供の流れも
「厨房で調理 → お盆を持って客の席まで歩く → 客の前で止まる → 皿を置く → 客が一瞬見る
（`LOOK_TIME`）→ 食べ始める」という間を明示的に入れており、料理がいきなりテーブルに
出現するような見え方にはなっていません。

### 役割が一目で分かる見た目
調理担当スタッフは白いコック帽、レジ担当スタッフは頭に巻いた小さなバンダナ状の帯を被り、
両者ともエプロンを着用します。客はこれらを着けないため、遠目にも「誰が客で誰がスタッフか」
「スタッフが何をしているか（調理中／レジ対応中）」が分かるようにしています。

### 店内オブジェクトのイラスト化
以前は「色付きの四角＋絵文字アイコン」でしたが、テーブル（皿つきの楕円形天板＋人数分の
チェア）・コンロ（バーナー2口）・仕込み台（まな板）・冷蔵庫（取っ手つきの縦長ボックス）・
レジ（カウンター＋レジスター本体）・観葉植物（鉢＋葉）・棚（仕切り線＋小物）を、それぞれ
複数の図形を組み合わせたコンパウンド形状として描き直しました。床も市松模様から木目調の
横板張りに、天井寄りには「奥の壁」の帯を追加し、入口には両開きドア＋「OPEN」のウェルカム
マットを配置しています。調理中は厨房の上に湯気のエフェクトが表示されます。

### ⚠️ 今回も仕様どおりにできていない点（正直に共有します）
- **カメラ視点**: ご依頼の「少し斜め上から店を覗いているような視点」は、今回は実装できて
  いません。現状は真上から見下ろす2D視点のままです。本格的な斜め見下ろし（疑似3D/アイソ
  メトリック風）にするには、座標系・当たり判定・描画順（奥行きソート）をすべて作り直す
  必要があり、今回のセッションでは「アニメーションの仕組みを先に完成させる」ことを優先した
  ため見送りました。次のステップとして着手可能です。
- **キャラクター素材**: Phase 2でお伝えした通り、実写・イラスト画像ではなく引き続き
  プログラムによるSVG合成です（環境にGPUが無く、画像生成モデルを実用的な速度で動かせな
  かったため、本セッションでは導入を見送りました。詳細は次節）。
- 表情は状態に応じた5パターン（普通／退屈／満足／怒り／食事中）に単純化しており、
  「料理を待つ間に周囲を見渡す」といった細かい仕草までは未実装です。

## 4-d. Phase 4で追加した「曜日ごとの再生・テキストと動きの同期」（今回の追加分の詳細）

「店舗の様子が全然どうなっているかが分かりません。曜日ごとに再生可能としてください。
テキストと連動するよう動くようにしてください。テキストの表示を3行として、テキストと
人の動きがわかるようにしてください（例：3名入店：3名がテーブルに着く）」というフィードバックを
受けて、`js/core/simulation.js`・`js/ui/scene.js`・`js/ui/field.js` の3ファイルを中心に、
テキストログと空間アニメーションの関係そのものを作り直しました。

### 根本原因：ログと空間演出が「別々の仕組み」で非同期に動いていた
Phase 2・3までは、テキストログ（`log`配列）と空間アニメーション用の構造化イベント
（`sceneEvents`配列）が別々に生成され、別々のペース（テキストは固定220ms間隔、
アニメーションは内部の「客グループ」タイマー）で再生されていました。そのため、
テキストでは「5名の料理が運ばれた」と表示されているのに、画面上ではまだ別の客が入店して
いる最中だったり、テキストが先に流れきってしまって店内アニメーションだけが後から追いつく、
といった「見た目とログが一致しない」状態になっていました。これが今回のフィードバックの
直接の原因です。

### 解決策：「beat」という単一の時系列データに統合
`simulation.js`の`runWeek()`を、`log`と`sceneEvents`という2つの配列を作る方式から、
`beats`というひとつの配列を作る方式に書き換えました。各beatは
`{icon, text, kind, day, action}` という形で、「表示するテキスト」と「そのテキストと
同時に店内で実行すべき演出アクション（`action`。ない場合は`null`）」と「どの曜日の
出来事か（`day`。0=準備、1〜7=月〜日、8=結果）」を1つのオブジェクトの中に必ずセットで
持ちます。`field.js`（新しく「ディレクター役」になった）は、beatを1つずつ取り出して
テキストを表示すると同時に`Game.UI.Scene.playBeat(action)`を呼ぶだけなので、
テキストと動きが構造的にズレようがない作りになっています。

具体的なbeatの例（ご依頼にあった「3名入店：3名がテーブルに着く」に対応するもの）：
- `enter_seat`（◯名が来店し、テーブルに着いた）→ 実際にその人数分のキャラクターが
  入口から歩いてきて着席する
- `serve_batch`（◯名の料理が運ばれた）→ 待っている客のうち該当人数分に、厨房スタッフが
  実際に料理を運ぶ
- `leave_waiting_batch`（◯名が待ちきれず、帰ってしまった）→ 該当人数の客が怒った表情で
  早歩きに店を出る
- `leave_disappointed`（◯名が来店したが売り切れで諦めて帰ってしまった）→ 該当人数の客が
  入店してすぐに引き返す

なお、数値計算（来客数・売上・利益など）のロジックは一切変更していません。旧`log`/
`sceneEvents`方式と新`beats`方式で7週間分をシード固定の乱数で計算し、すべての数値
結果が完全一致することを確認した上で反映しています（表示方法だけを作り直した変更です）。

### 曜日ごとの再生
FIELD画面に「準備／月／火／水／木／金／土／日／結果」の9個のタブを追加しました。
- 任意のタブをクリックすると、他の曜日を経由せずに即座にその曜日の頭から再生されます。
- 1つの曜日の全beatを再生し終えると自動的に一時停止し、（結果画面でなければ）
  「▶ 次の日へ進む」というボタン表示に変わります。
- 「◀ 前の日」「次の日 ▶」ボタンでも1日ずつ移動できます。
- 「⏭ 結果を見る」を押すと残りの曜日をすべて飛ばして結果パネルまで直接ジャンプします
  （このときの「提供／離脱／売切商品」の数字は週全体の合計値に切り替わります）。

### 割り切った簡略化点（正直に共有します）
各曜日に切り替わるたびに、店内の客キャラクターは全員リセットされ、空の状態から
その曜日のbeatを再生し直す仕様にしています（スタッフは配置されたまま）。つまり
「月曜日の営業が終わった状態のまま火曜日が始まる」という客の引き継ぎはありません。
これは、どの曜日にも自由にジャンプできるようにするための意図的な割り切りです
（過去の曜日を毎回最初から再生し直す必要が無く、身軽に動きます）。数値計算・結果の
集計には一切影響しません。

### 動作確認
Playwrightで、営業開始→曜日タブが9個正しく描画される→準備日の再生→
「次の日へ進む」で月曜日に自動遷移→テキスト「◯名が来店し、テーブルに着いた」の
表示と同時にDOM上のキャラクター数が実際に増える（3→7→11のように、テキストの
人数分だけ増えることを確認）→任意の曜日タブ（木曜）へのジャンプ→
「結果を見る」での即時ジャンプと合計値表示、という一連の流れをコンソールエラー
ゼロで確認しました。

## 4-e. Phase 5・ステージ1で追加した「実PNG素材によるキャラクター/店舗表示」（今回の追加分の詳細）

「`chibi_restaurant_assets.zip`を正式なプロトタイプ素材として使い、自分で画像を生成したり
仮素材で代用したりせず、実際にPNGを読み込んで組み合わせてキャラクターと店舗を表示してほしい。
ドラクエ的なマス目移動ではなく、客とスタッフがそれぞれ状態を持って自律的に働いているように
見せてほしい」というご依頼を受けて着手しました。ご依頼の進め方（ステージング）どおり、
まず「客1人・スタッフ1人・テーブル1・椅子1・厨房1・料理1種」だけで入店〜退店の1ループを
完成させ、動作確認したのが今回の到達点です。**既存のOFFICE/LAYOUT/FIELD本編（Phase 1〜4）は
まだ変更していません**（次段階で複数客・経済シミュレーションと統合する前提の土台作りです）。

### ZIPの中身
全68ファイル、すべて128×128のRGBA透過PNG。
- `characters/`: `skin`(4トーン: skin/skin1/skin2/skin3) `ears_basic` `hair_brown` `shirt_blue`
  `pants_navy` `shoes_brown` `eyes_basic` `nose_basic` `mouth_smile` の9カテゴリ×4方向
  （down/up/left/right）。レイヤー順は同梱の`README.txt`に明記されており、その順序
  （skin→ears→hair→clothes→pants→shoes→eyes→nose→mouth）をそのまま採用しています。
- `furniture/`: entrance（ドア）table_4seat（4人テーブル）chair（椅子）kitchen_stove（コンロ）
  fridge（冷蔵庫）counter（カウンター）register（レジ）plant（観葉植物）
- `food/`: ramen pasta parfait hamburger curry_rice
- `ingredients/`（現状未使用・将来の商品開発機能向け）: egg lettuce meat noodle tomato
- `ui/`（現状未使用・将来アイコン置き換え向け）: coin heart

### 新設したファイル（既存ファイルは未変更）
- `assets/`: ZIPの中身をそのままコピーしたPNG本体（`assets/characters/...` `assets/furniture/...` 等）。
- `js/data/assetManifest.js`: 画像パスを直書きしないためのアセット管理層。`CharacterAssets`
  （パーツID・レイヤー順・方向・パス生成関数・見た目のランダム/既定生成）、`FurnitureAssets`、
  `FoodAssets`、`UiAssets`、`IngredientAssets`、`AnimationDefinitions`（状態→アニメ種別の対応表）
  をここに集約。パーツを追加・差し替える場合はこのファイルだけを編集すればよい構造です。
- `js/core/actorFSM.js`: Customer 10状態・Staff 10状態（ご依頼どおりの一覧）を純粋なロジックとして
  実装。画像・DOM・SVGへの参照は一切なし。「時間経過で進む状態」（duration+onTimerDone）と
  「移動完了／相手からの合図で進む状態」（onEnter+コールバック）の2種類の遷移を汎用FSMランナー
  （`setState`/`tick`）でまとめて扱っています。誰がどの客を担当するかの割り当て（`dispatch`）も
  ここに集約。
- `js/ui/actorSprite.js`: PNGレイヤー合成の描画専用エンジン。状態（`animKind`）と位相
  （`phase`/`idlePhase`）から「そのフレームでの見た目のブレ（上下のバウンド・回転）」だけを
  計算し、9枚の`<image>`を重ねて描画します。ロジック（actorFSM.js）は一切知りません。
- `js/ui/sceneV2.js`: 上記2つを結び付ける「世界」。固定の店内配置（家具の座標）・移動（現状は
  直線移動、下記の簡略化点を参照）・誰が誰を担当するかの毎フレーム判定・PNG描画の呼び出しを
  行います。
- `asset_test.html`: 上記エンジン単体の検証用ページ（本編のindex.htmlとは独立。客1人・
  スタッフ1人のループを自動で繰り返し再生し、下部に現在の状態をテキスト表示するデバッグ機能
  付き）。

### レイヤー合成の仕組み
各キャラクターは、128×128キャンバス上で9枚のPNG（`skin→ears→hair→clothes→pants→shoes→
eyes→nose→mouth`）を`<image>`要素としてそのまま重ね、拡大縮小や位置の個別調整は一切行って
いません。方向（direction）が変わると、9枚すべてが対応する方向の画像に差し替わります
（README.txt指定どおり、パーツごとの基準キャンバス・アンカーは変更していません）。「髪だけ
差し替え」「服だけ差し替え」は、`assetManifest.js`の`CharacterParts.hair.options`等に新しい
partIdを1行追加するだけで対応できる構造です。

### アニメーションの仕組み（画像切り替えではなく連続的なブレ）
パーツ画像自体は静止画なので、歩行・食事等の「動き」は、状態ごとに定義したアニメ種別
（`AnimationKind`: WALK / IDLE / IDLE_ALERT / SIT_IDLE / SIT_TALK / SIT_EAT / STAND_TALK /
WALK_CARRY / COOK）に応じて、キャラクター全体を包む`<g>`への小さな平行移動・回転を
`Math.sin`ベースの連続位相で計算することで表現しています（既存のSVGキャラクターエンジンと
同じ「連続位相アニメーション」の考え方を踏襲）。例えば歩行時は2歩で1往復するバウンドと左右の
揺れ、食事中は一口ごとの小さな頷き、調理中は小刻みな体の揺れ、というように。手に持っている
もの（料理・お金）は、実際の`food/*.png`・`ui/coin.png`を小さく重ねて表示します。

### Customer / Staff の状態遷移（ご指定どおり実装）
- Customer: ENTERING → LOOKING_FOR_SEAT → WALKING_TO_SEAT → SITTING → WAITING_FOR_ORDER →
  ORDERING → WAITING_FOR_FOOD → EATING → PAYING → LEAVING
- Staff: IDLE → NOTICE_CUSTOMER → WALKING_TO_CUSTOMER → TAKING_ORDER → WALKING_TO_KITCHEN →
  COOKING → CARRYING_FOOD → SERVING → CASHIER → RESTING（RESTING/IDLEは次の客を待つ間で循環）
`Customer.state`と`Staff.state`はそれぞれの実体（entity）が持つ単純な文字列であり、
表示側（actorSprite.js）はそれを読むだけで「何を描くか」を決めます（ゲームロジックと表示の分離）。

### 意図的な簡略化（正直に共有します）
- **移動経路**: 今回のステージ1では、既存の`core/pathfind.js`（グリッドBFS経路探索）を
  使わず、地点間の直線移動にしています。客1人・スタッフ1人・家具最小構成の検証専用配置で
  障害物を避ける必要が無いためと、まず表示とFSMの結線を確実に動かすことを優先したためです。
  次段階でLAYOUT画面（グリッド配置）・複数客に対応する際は、既存のBFS経路探索に接続し直す
  想定です。
- **カメラ視点**: 今回も真上からの2D視点のままです（Phase 3・4から継続の未対応点）。ただし
  家具を全てキャラクターより背面に固定表示することで、「キャラクターがテーブルなどに隠れて
  見えなくなる」ことは無いようにしています（ご依頼11番の優先順位どおり）。
- **本編未統合**: 現時点ではOFFICE→営業開始→FIELDという既存の経済シミュレーションのフローとは
  まだ接続していません（`asset_test.html`という独立した検証ページのみ）。複数客・経済計算との
  統合、および既存FIELD画面（Phase 4のbeat/曜日タブ）との統合は、今回の動作確認を経てから
  次段階で着手します。

### 不足している素材（正直に共有します・代用は一切していません）
| 用途 | 不足内容 | 必要な方向 | 必要な理由 |
|---|---|---|---|
| 表情差分 | `eyes`は`eyes_basic`のみ、`mouth`は`mouth_smile`のみ | down/up/left/right | 「待っていて退屈そう」「怒って帰る」等、状態に応じた表情の視認性（ご依頼6番） |
| 食事の口の動き | `mouth`の開閉差分（例: `mouth_open`） | down/up/left/right | 食事アニメーションを頷き以外の動きでも表現するため |
| スタッフの役割衣装 | コック帽・エプロン（調理担当）、バンダナ等（レジ担当）に相当するパーツ | down/up/left/right | 客とスタッフ、調理担当とレジ担当を一目で区別するため（ご依頼冒頭の目標に関連） |
| 髪型・髪色バリエーション | `hair_brown`以外の髪型・色 | down/up/left/right | 複数客を同時に表示した時に見分けが付くようにするため |
| 服・ズボン・靴の色バリエーション | `shirt_blue`/`pants_navy`/`shoes_brown`以外の色 | down/up/left/right | 同上（男性客／女性客／子供等の演出にも活用予定） |

これらが無くても「客1人・スタッフ1人」の動作確認自体は完了できていますが、次段階（複数客の
同時表示・役割の視認性・ボトルネックの視覚化）には必要になります。追加素材をいただき次第、
`assetManifest.js`の該当カテゴリに1行追加するだけで反映できる構造にしてあります。

### 動作確認
Playwrightで`asset_test.html`を60秒以上動かし、Customer/Staffともに指定された全状態
（Customer 10種・Staff 10種）に実際に遷移すること、1人目の客が退店した後に2人目の客が
自動的に入店してループが継続すること、コンソールエラー・ネットワーク読み込み失敗が
ゼロであることを確認しました。またスクリーンショットで、入店・着席・注文・調理・配膳・
食事・会計の各局面でキャラクター同士が過度に重ならず、料理アイコンや吹き出しで何が
起きているか見て取れることを確認しています。

## 4-f. Phase 5・ステージ2「高速化＋複数客ボトルネック可視化＋v2素材差し替え」（今回の追加分の詳細）

### 1. 速度改善（「動きがもっさりしている」フィードバックへの対応）
「一呼吸」的な間（着席後の間・注文のやり取り・調理時間等）を一括で調整できるよう、
`actorFSM.js`に`PACE`という倍率定数（最終値0.28）を導入し、すべての`duration`にこれを
掛ける形にしました。あわせて歩行速度・歩行アニメーションの周期・アイドル時の揺れの周期
（`sceneV2.js`の`WALK_SPEED`/`WALK_PHASE_RATE`/`IDLE_PHASE_RATE`）も引き上げています。
「間の長さ（PACE）」と「歩く速さ（WALK_SPEED等）」を別の定数に分けているのは、今後
「テキパキ感」と「物理的な歩行速度」を独立に調整できるようにするためです。Playwrightで
2人目の客が出現するまでの実測時間が改善前後でどう変わったかを計測し、体感の速さが
実際に短縮されたことを確認しました。

### 2. 複数客の同時進行とボトルネックの可視化（ご依頼9・10番）
- テーブルを1卓→3卓（`SEATS`配列、各卓に客側の`seatPoint`とスタッフ側の`servePoint`）に拡張し、
  `claimSeat`/`releaseSeat`で空席管理をするようにしました。
- 客は一定間隔（0.9〜1.9秒のランダム間隔）で自動的に来店するようにし（`scheduleNextArrival`）、
  店内に同時に存在できる客の上限を6人としています。
- **数字やログではなく「実際の行動」でボトルネックを見せる**ため、Customerの状態遷移表に
  以下の「諦めて帰る」経路を追加しました（新しい状態は一切追加せず、ご指定の10状態のみで
  表現しています）。
  - `LOOKING_FOR_SEAT`: 満席なら少し待って再挑戦し、約9回（≒4秒弱）試しても座れなければ
    諦めて`LEAVING`へ。
  - `WAITING_FOR_ORDER`: スタッフが来る前に一定時間（6.5秒）待たされたら、席を解放して
    `LEAVING`へ。
  - `WAITING_FOR_FOOD`: 料理が来る前に一定時間（8秒）待たされたら、同様に`LEAVING`へ。
  - スタッフが1人しかいないため、来店ペースに対して接客・調理・配膳・会計のすべてを
    こなしきれず、上記の経路が実際に発動して客が入れ替わり立ち替わり帰っていく様子が
    画面上で確認できます（下記GIF参照）。

### 3. 見つけて直したバグ（正直に共有します）
複数客対応のテストで、**客が会計（PAYING）のまま永久に取り残されて退店しない**という
不具合を発見しました。原因は、レジ担当への割り当て処理（`dispatch`内）が`ctx.moveTo`だけを
直接呼び出し、スタッフの状態（`ent.state`）を"IDLE"のまま変えずにレジへ歩かせていたことに
あります。歩いている間ずっと"IDLE"に見えてしまうため、その隙に別の客の注文対応へ同じ
スタッフが二重に割り当てられてしまい、最初の客の会計処理（お金を受け取って`LEAVING`にする
コールバック）が握りつぶされていました。
修正は、レジ担当への割り当て時に`setState()`で即座に`CASHIER`状態へ遷移させ（歩いている間も
"IDLE"に戻らないようにする）、`CASHIER`状態の内部でも「レジへの移動」と「会計の間」を
明確に分離（到着後に改めて計測を開始する）する形にしました。あわせて、接客中に客が諦めて
帰ってしまった場合（調理中・配膳中に相手が`LEAVING`になっているケース）にスタッフが空の
テーブルへ向かったり、皿を出しっぱなしにしたりしないよう、`isServableCustomer()`という
判定を各引き渡しポイントに追加しています。60秒間のPlaywright実行で、customer idが
途切れず（34人分）入れ替わり続けること、コンソールエラー・404（favicon以外）がゼロで
あることを確認しました。

### 4. `chibi_restaurant_assets_v2.zip`への素材差し替え
ユーザーから新しく提供されたv2素材を、正式なプロトタイプ素材として全面的に採用しました。
- **座標系はv1から変更なし**: 128×128 RGBA、全レイヤーを(0,0)へそのまま重ねる方式は
  そのまま継続できるため、`actorSprite.js`（レイヤー合成・アンカー計算）は無修正です。
  README.txt記載の基準座標（頭中心(64,43)・目y36-44・鼻y46-50・口y51-59・服y56-91・
  ズボンy84-112・靴y105-120）を実測（PILでの`getbbox()`）した結果、実際のパーツ配置と
  ほぼ一致していることを確認済みです。
- **ID・命名の変更**（`assetManifest.js`のみ修正、ロジック側は無修正）:
  - `clothes`: `shirt_blue` → `clothes_blue`
  - `ears`: `ears_basic` → `ears`
  - `nose`: `nose_basic` → `nose`
  - `skin`: バリエーション`skin1`/`skin2`/`skin3`が廃止され、`skin`の1種のみに
  - `hair`: 新たに`hair_dark`が追加され、`hair_brown`と合わせて2種に
- 上記に伴い、`assets/`以下の古いファイル（`skin1_*`〜`skin3_*`、`shirt_blue_*`、
  `ears_basic_*`、`nose_basic_*`）はv2に存在しないため削除し、v2のファイルに一本化しました。
- `food`/`ingredients`/`ui`の全ファイル、および`furniture`の大半（entrance以外）もv2のものに
  差し替えています（ファイル名は同じでも中身の絵は新しくなっています）。

### 不足している素材（v2でも未解決・代用はしていません）
| 用途 | 不足内容 | 必要な理由 |
|---|---|---|
| **入口ドア（furniture/entrance.png）** | v2 ZIPに同梱されていません | 店の入口が描画できないため、**暫定的にv1のentrance.pngをそのまま流用しています**。差分が届き次第差し替えます |
| 表情差分 | `eyes`/`mouth`とも1種類のみ | 「待っていて退屈そう」等の状態を表情でも表現するため（4-e節から継続） |
| 食事の口の動き | `mouth`の開閉差分 | 食事アニメーションの表現力向上のため（4-e節から継続） |
| スタッフの役割衣装 | コック帽・エプロン等 | 客とスタッフ、調理担当とレジ担当の見分けのため（4-e節から継続） |
| 服・ズボン・靴の色バリエーション | `clothes_blue`/`pants_navy`/`shoes_brown`以外の色 | 複数客が同時に画面にいる時に見分けが付くようにするため（4-e節から継続、v2でも未解決） |

### 動作確認
Playwrightで`asset_test.html`を60秒間動かし、以下を確認しました。
- Customer 10状態・Staff 10状態（NOTICE_CUSTOMER含む）すべてへの遷移
- 最大6人の客が同時に異なる状態（入店中・席探し中・注文待ち・食事中・会計中等）で
  存在すること
- 60秒間で34人分のユニークな客IDが入れ替わり立ち替わり店に出入りしたこと（＝ループが
  破綻せず回り続けている）
- 満席・スタッフ不足による「諦めて帰る」客が実際に発生すること
- コンソールエラー・ネットワーク読み込み失敗がゼロ（favicon.icoの404のみ、ゲームには
  無関係）
- スクリーンショット・GIFで、複数の客が同時に別々の行動をしている様子と、席が埋まって
  入店直後の客が立ち往生する「混雑」が実際に見て取れること

## 4-g. Phase 5・ステージ3「素材の抜本的見直し（LimeZu購入・LimeZu素材への全面差し替え）」（今回の追加分の詳細）

添付いただいた参考画像（陰影のある塗り・役職差のある衣装・什器の作り込み）レベルを
目指すため、「根本から考え直す」方針でキャラクター素材そのものを再検討しました。

### 1. Universal LPC Spritesheet Character Generatorの検証と不採用
無料・改変自由（CC-BY-SA等）でレイヤー分割・4方向対応の定番素材として検証しましたが、
実際にPythonで合成した比較画像で、体型が「等身の高い一般的なRPG体型」であり、本作が
必要とする「3頭身デフォルメ」と根本的に合わないことが判明しました（頭身比率は衣装や
髪型を変えても解決しない、ボディ自体の問題）。技術的な合成手順自体は正しく動作した
ため、素材の入手経路としては今後も選択肢になり得ますが、今回は不採用としました。

### 2. LimeZu「Modern Interiors」の購入
3頭身・レイヤー分割・4方向という条件で再度探し、LimeZuの「Modern Interiors -
RPG Tileset」（itch.io、名前設定価格・$1.50以上で完全版）を、ブラウザで実際の
プレビュー画像を目視確認した上で選定しました（ユーザー様が購入・
`C:\Clode\スキル経営ゲーム\ModernItemInterior\`に配置）。

### 3. 【重要・正直に共有します】素材を分解して分かった制約：「上向き（後ろ姿）」が無い
購入素材を解凍し、ピクセル単位の画像解析（透明度の境界を検出してコマ割りを特定する
スクリプト）で構造を調べたところ、次のことが分かりました。

- パック本体の「Character_Generator」（Body/Outfit/Hairstyle/Eyes/Accessoryを番号で
  選んで組み合わせる新方式）は、どのカテゴリも**正面（下向き）専用**の作り込まれた
  アニメーション集（idle・歩行・睡眠・着席・電話・カート押し・贈り物・持ち上げ・
  投げる・殴る・刺す・銃・被弾等、19種類）でした。Body/Outfit/Eyes/コック帽
  アクセサリのシートを個別に検証しましたが、どれも同一の「正面専用19行」構成で、
  左向き・右向き・後ろ向きの行は存在しませんでした。
- パックに同梱されている旧フォーマット「Single_Characters_Legacy」（Adam/Bob/Lucy等
  20体の完成済みキャラクター）には、下向き・左向き・右向きの歩行アニメーションが
  ちゃんと存在します。ただし、これも全9種類のアニメーション行をすべて確認しましたが、
  「後ろ向き（上向き）」の行だけは見つかりませんでした。
- 一方でLimeZu公式のプレビュー画像（OUTFIT_LIST.png / ACCESSORY_LIST.png）には
  左・後ろ・右・正面の4方向が完成イラストとして存在します。これはおそらく
  Windows専用の「Character Generator 2.0」ツール内部でのみ生成・保持されている
  データで、購入した静的な画像ファイルとしては同梱されていません（Wine経由での
  実行も検討しましたが、Linux上でWindows GUIツールを動かす賭けになるため見送り、
  ユーザー様に判断を仰ぎました）。

この結果を包み隠さずご報告し、「①レイアウトを再設計して上向き移動自体を無くす」
「②上向きの代わりに左右いずれかの画像を代用する」「③Wine経由でツールを動かす
賭けに出る」「④他の入手方法を探す」の4案を提示したところ、**②（左右画像で代用）**
を選んでいただきました。この方針にもとづき、`assetManifest.js`の
`UP_DIRECTION_FALLBACK`で「up」方向の要求時は機械的に「left」の画像を返す実装に
しています（見た目としては、上方向へ歩くキャラクターは終始左を向いたまま移動します。
本物の後ろ向き素材が手に入り次第、ここだけを差し替えれば直せる作りにしてあります）。

### 4. 役割ごとの見た目（キャラクター選定）
Legacy素材は「パーツを重ねる」方式ではなく「完成済み1枚絵」のため、v1/v2までの
「skin+hair+clothes+…をレイヤー合成する」方式をやめ、**役割ごとに1つの完成画像
（方向×4）を選ぶだけ**の方式に変更しました（`js/data/assetManifest.js`の
`CHARACTER_LOOKS`、`js/ui/actorSprite.js`の描画部分を全面書き換え）。カスタマイズ性
（パーツの自由な組み合わせ）は失いますが、「実在する素材だけを使う」という大原則を
優先しました。

- **客**: Adam / Bob / Lucy / Molly / Alex / Amelia / Ash / Bruce / Dan / Edward /
  Pier / Rob / Roki / Samuel の14種類からランダムに選び、来店ごとに見た目が変わる
  ようにしています。
- **従業員（調理・接客・レジを1人で兼務）**: Legacy素材の中に、既製の役職差分
  「Chef_Alex」「Chef_Molly」等（コック帽＋エプロン）と「Bouncer」（フォーマルな
  黒服・サングラス）を発見したため、一目で「店員」と分かるよう**コック帽姿の
  "Chef_Alex"をデフォルト採用**しました。
  - ⚠️正直な注記: "Chef_Alex"は前述の「正面専用」制約の対象で、**下向きの絵しか
    存在しません**。左右へ歩く場面でも同じ正面向きの絵をそのまま使っています
    （不自然にコック帽から別キャラに切り替わるより、コック帽姿を保つことを優先した
    判断です）。より自然な向き替えが必要になった場合は、方向を持つ"Bouncer"を
    従業員側の見た目として使う選択肢もあります（`STAFF_LOOKS.service`として
    既に用意済み）。
  - ⚠️「ウェイター服」に相当する素材はこのパック内に見当たりませんでした
    （ロング丈の33種の私服中心で、給仕用の制服という括りは無いようです）。
    無い物は作らない方針のため、現状は上記のコック帽／フォーマル黒服のいずれかで
    代用しています。

### 5. 描画エンジンの変更点
- キャンバスサイズを128px→32px（LimeZu素材のネイティブサイズ）に変更し、
  `DISPLAY_SCALE`を0.34→1.4（縮小→拡大）に変更（見た目の大きさは旧v2とほぼ揃えて
  あります）。
- ドット絵が滲まないよう、`#scene-svg image { image-rendering: pixelated; }`を
  `css/style.css`に追加しました（32pxを1.4倍に拡大表示するため、この指定が無いと
  輪郭がぼやけて「チープな見た目」に見えてしまいます）。
- 状態遷移・当たり判定・待ち行列などのゲームロジック（`core/actorFSM.js`）は
  今回まったく変更していません（見た目の差し替えのみ）。

### 動作確認
Playwrightで`asset_test.html`を60秒間動かし、以下を確認しました。
- 新しいキャラクター画像（客14種＋コック帽の従業員）が正しく表示され、コンソール
  エラー・404が（favicon.ico以外）ゼロであること
- 60秒間で33人分のユニークな客IDが入れ替わり立ち替わり店に出入りし、4-f節で確認
  済みの挙動（満席・注文待ちタイムアウトでの離脱等）に見た目差し替えによる新規の
  不具合が発生していないこと
- スクリーンショット・GIF（`demo_v3.gif`）で、色分けされたブロック的な見た目から
  実際の顔・髪型・衣装が見えるチビキャラクター表示に変わったことを確認

## 4-h. Phase 5・ステージ4「コック二重表示の再修正・多フレームアニメ化・軸移動化・什器実素材化」（今回の追加分の詳細）

4-g節の素材差し替え後に頂いた「①コックが二人で動いている②上向き素材の有無を全キャラ
再確認③歩行/停止アニメをしっかり実装④斜め移動を禁止⑤テーブル・椅子を素材から配置
⑥テーブルに重ならない経路移動⑦店内装飾も素材から設定⑧高速動作も担保」という8点の
フィードバックに、1つずつ対応しました。

### 1. 「コックが二人」バグ：実は2箇所に同じ種類のバグがあった
まず、直前のご報告時点で疑っていた「idle行の切り出しピッチ（1コマの幅）取り違え」を
`staff_chef_down_*`について修正済みでしたが、実際にブラウザで再現テストしたところ、
**下向きは直ったものの、左右向き（＝上向きの代用先を含む）でまだ頭が2つに見える
バグが残っていました**。原因を1枚ずつ画像を目視して特定したところ、"Chef_Alex"は
正面専用（左右向きの絵が存在しない）キャラのため、本来は正面向きの絵をそのまま
コピーして使うべきところ、抽出スクリプトが誤って別の行から切り出してしまい、
やはり2コマ分が1枚に混ざった画像になっていました。**この左右向き分だけ、
確実に正面向きの完成コマをそのままコピーする方式に直し**、Playwrightで卓ごとに
コックが調理・配膳している場面を複数回スクリーンショットして、頭が1つだけに
なっていることを目視で確認しました。

### 2. 上向き素材の有無：全キャラを対称性スコアで再確認済み（結果は変わらず）
「本当に無いか他のキャラも含めて確認してください」とのご指摘を受け、客14種＋
Bouncer＋Chef_Alexの**全15キャラ**について、シート全行の左右反転との重なり率
（対称性スコア）をピクセル単位で計測しました。「正面向き」は0.7〜0.8、
「左向き/右向き」は0.34〜0.49という値に全キャラ一貫してきれいに分かれており、
後ろ向き特有の非対称パターンはどのキャラにも見つかりませんでした。つまり
**「後ろ向き素材が存在しない」という結論は変わりません**。この再確認結果を踏まえ、
上向きの代用先を`UP_DIRECTION_FALLBACK`で「left」から**「right」**に変更しました
（見た目の印象を変えるための対応で、素材そのものが増えたわけではありません）。

### 3. 歩行・停止の多フレームアニメーション
これまでは方向ごとに静止画1枚を表示し、「揺れ」（上下移動・回転）だけで動きを
表現していましたが、これだと停止中は完全に静止画のままでした。今回、
`assetManifest.js`に方向×動作種別（歩行/待機）ごとのコマ数テーブルを追加し
（歩行6コマ、待機は下向きが本物の呼吸モーション4コマ、左右向きは歩行コマから
2枚抜粋）、`actorSprite.js`が経過時間から今表示すべきコマ番号を計算して切り替える
方式に変更しました。歩行中と停止中とで別々のコマ集合・別々の周期（`walkPhase`/
`idlePhase`）を使うため、**停止していてもコマが順に切り替わり続け、静止画のまま
にはなりません**。既存の「揺れ」演出（`computeAnimOffset`）はそのまま残してあり、
コマ切り替えと揺れが両方同時にかかる形です。Playwrightでコック・客それぞれの
`<image>`の`href`属性を高頻度で読み取り、歩行中は6種類、停止中は2〜4種類の
ファイル名が周期的に入れ替わることを確認しました。

### 4. 斜め移動の禁止（軸移動のみ）
これまでは目的地への直線ベクトルでそのまま移動していたため、x・yが同時に変化する
「斜め歩き」になっていました。今回、目的地までの移動を**必ずdxかdyのどちらかが
0になる区間の連なり**に分解する`planAxisPath()`を新設し（詳細は次項）、
1フレームごとの位置更新も「dxが0でなければxだけ動かす、そうでなければyだけ動かす」
という単純な分岐にしました。Playwrightで複数キャラが同時に動いている状態を
アニメーションフレーム単位（`requestAnimationFrame`ごと）でサンプリングし、
480フレーム分（客・スタッフ合計）を調べて**斜め移動が1件も発生していない**ことを
確認しました（0.05秒間隔の粗いポーリングでは経路の曲がり角をまたいで斜めに
「見えてしまう」誤検出があったため、確実性のためフレーム単位の検証に切り替えて
います）。

### 5. テーブル・椅子・その他什器のLimeZu実素材化
これまでプレースホルダのまま残っていた什器画像（`assets/furniture/`）を廃止し、
「Modern Interiors」同梱の`1_Interiors/16x16/`から実素材を採用しました
（`assets/furniture_v3/`）。テーブル・コンロ・冷蔵庫・調理台・レジは
`Theme_Sorter_Singles`フォルダの切り出し済み単体画像をそのまま使用、椅子・入口
ドアは切り出し済み版が存在しなかった`1_Generic_16x16.png`から透明度の
連結成分検出で矩形を特定してクロップしました（詳細は`assetManifest.js`の
コメント参照）。

### 6. テーブルとの衝突を避ける経路移動（最短距離は保証しない簡易ルール）
3卓とも同じ高さの範囲（テーブルの天面〜椅子の脚元）に収まっている店内レイアウトの
特性を利用し、「①横移動はこの高さの範囲の外でしか行わない②その範囲を縦に
通過する時は必ずテーブルとテーブルの隙間（通路）のx座標を通る③自分の目的地
であるテーブル自身への最後の直進だけは例外的に許可する（配膳・着席という
演出上意図した重なりのため）」という3つのルールだけで、最短距離ではないものの
**必ずどのテーブルにも横から重ならない**経路を`planAxisPath()`で組み立てています。
移動開始時に1回だけ経路を計算して待ち行列（`ent.pathQueue`）に積む方式のため、
経路計算自体が毎フレーム走るわけではなく性能への影響もありません。Playwrightで
客・スタッフが行き交う様子を20秒間・1200フレーム分サンプリングし、「テーブル/椅子の
矩形と重なっているのに、そのx座標が重なっている当のテーブル自身の中心xではない」
というケース（＝他人のテーブルを横切ってしまっているケース）が**0件**であることを
確認しました。

### 7. 店内装飾の実素材化
壁の額縁2枚・床のラグ1枚を`1_Generic_16x16.png`から追加しました（当たり判定は
持たせておらず、経路計画にも一切関与しません＝純粋な見た目の装飾です）。

### 動作確認（今回分）
- `node --check`で`assetManifest.js`・`actorSprite.js`・`sceneV2.js`の構文確認。
- Playwrightで`asset_test.html`を起動し、コンソールエラー・異常な404が
  （favicon.ico以外）ゼロであることを確認。
- 上記1〜6の各項目について、それぞれ専用の検証スクリプトで個別に確認（コックの
  頭が1つであることの目視確認、歩行/待機コマの周期的切り替えの確認、斜め移動
  0件の確認、テーブルへの横からの重なり0件の確認）。
- **性能**: `requestAnimationFrame`のフレーム数を60秒間カウントし、平均
  **60.0fps**を維持できることを確認（同時に32人分のユニーク客IDが入れ替わり
  立ち替わり店に出入りする負荷条件下）。軸移動化・什器増加・多フレーム
  アニメーションを追加した後でも速度低下は発生していません。
- スクリーンショット・GIF（`demo_v3b.gif`）で、実物のテーブル・椅子・厨房什器・
  装飾に囲まれて、コックが1人だけの姿で調理・配膳し、客が歩行/着席それぞれで
  コマが切り替わりながら過ごす様子を確認。

## 4-i. Phase 5・ステージ5「4人がけテーブル化・コック/ウェイター2人体制化・配膳/会計演出強化」（今回の追加分の詳細）

「①テーブルを縦に長く②椅子は左右のみ・左右2つずつ③料理をテーブルに置く④キッチンを
広くしてカウンター経由でウェイターが受け取る⑤ウェイターは黒服の人でよい⑥レジを入口近くに
置き会計金額をポップアップさせる」という6点のフィードバックに対応しました。②と④について
は、見た目だけの変更か実質的な仕様変更かが依頼文からは判断できなかったため、着手前に
`AskUserQuestion`で確認し、**どちらも「大きい方（実質変更）」を選んでいただきました**
（1卓最大4人まで同時着席／コックとウェイターを別々の2人体制）。以下はその前提での実装です。

### 1. テーブルを縦長の実素材に差し替え（ピクセル絵を歪ませない作り方）
単純にテーブル画像を縦方向に引き伸ばすと、木目やテーブルの脚の比率が崩れて「間延びした
絵」になってしまいます。そこで、素材の16×16ネイティブ画像（`Kitchen_Singles_275.png`）を
行単位で分析し、「上端の輪郭1行・繰り返し可能な板目8行・下端の輪郭＋脚5行・透明1行」という
構造を特定した上で、**繰り返し可能な板目部分だけをタイル状に継ぎ足して**縦に長いネイティブ
画像（16×30）を作り、そこから3倍拡大（`Image.resize(..., NEAREST)`）して最終画像
（48×90px）を生成しました。脚は下端にしか出ないため、間延びせず自然な縦長テーブルに
なっています（`assets/furniture_v3/table.png`を上書き）。

### 2. 1卓4席（左右2脚ずつ）・実質12席への拡張
`sceneV2.js`の`SEATS`を3席から**12席**（3卓×4席）に拡張しました。各卓につき、卓の中心から
左右に34px離れた列に、上段・下段の2脚ずつ椅子（＝座席）を配置しています（上下には椅子を
置かない＝「椅子は左右にのみ並ぶ」というご要望どおり）。`claimSeat`は12席のうち空いている
ものを1つ確保するだけなので、複数の客が同時に別の椅子に座ることができ、実際に**1卓に
最大4人まで同時着席する**ことをPlaywrightで確認しています（下記「動作確認」参照）。

### 3. 料理を卓上の固定点に置く（客について回らない）
これまで料理アイコンは配膳された客の頭上に表示され、客が動くとついて回っていました。
今回、各座席データに`dishPoint`（卓の内側、椅子の位置とは別の固定座標）を追加し、
`showDish()`がそこに料理を描画するように変更しました。一度置かれた料理は、客が食べ終えて
`clearDish()`されるまで、客が動いても位置が変わりません（Playwrightで、同じ客が
`EATING`状態の間、料理の描画座標が完全に固定されたままであることをフレーム単位で確認）。

### 4. キッチンを広げ、コック/ウェイターの2人体制に分離
これまでは1人のスタッフが「注文を取る→厨房で作る→運ぶ→会計する」を兼務していましたが、
今回、役割を完全に分離しました。
- **コック**（`role: "cook"`、見た目は従来どおりコック帽の"Chef_Alex"）: コンロの前
  （`stovePoint`）に立ったまま、完成した料理をカウンター（`counterPoint`）に置きに行く
  往復以外は一切客席側に出ません。
- **ウェイター**（`role: "waiter"`）: 接客・カウンターでの受け取り・配膳・会計を担当し、
  コンロには一切近づきません。
両者は直接お互いの状態を参照せず、`sceneV2.js`が持つ「厨房の注文キュー」（ウェイターが
注文を取ったら積む→コックが取り出して調理開始）と「カウンターの受け取り待ちキュー」
（コックが調理完了時に積む→ウェイターが取り出して配膳）という2つの共有キューだけで
間接的にやり取りする疎結合な設計にしています（`core/actorFSM.js`を`CookState`/
`WaiterState`という2つの状態遷移表に分割）。あわせてキッチンの什器を横に広げ、コックと
ウェイターの受け渡し場所となる横長のカウンター什器（幅74px、従来比+85%）を新設しました。

### 5. ウェイターの見た目
ご要望どおり「黒い服を着た人」として、4-g節で導入済みのフォーマルな黒服見た目
"Bouncer"（`STAFF_LOOKS.waiter`）をそのまま採用しました。専用のウェイター制服素材は
このパックには無いため（4-g節で既報告）、新規に何かを作ることはせず、既存の実素材を
役割に転用する形です。この見た目はコックの"Chef_Alex"と違い元々左右の向き別の絵を
持っているため、接客時に客の方を向く動きも自然に表現できています。

### 6. レジを入口近くに配置し、会計金額のポップアップ演出を追加
レジ什器・客とウェイターがレジで向き合う地点（`registerPoint`）を、これまでの店の奥
（右上）から**入口のすぐそば**（左手前）に移動しました。会計が完了した瞬間
（ウェイターの`CASHIER`状態が完了した時点）に、`showPayment()`がその客の注文の価格
（既存の`js/data/products.js`の`basePrice`を流用。「パフェ」はproducts.js側では
「デザート」というid名のため、その価格を充てています）を「¥800」のような文字列として
レジの位置から生成し、約1.1秒かけて上へふわっと浮かびながら消えるアニメーション
（`moneyPopups`配列、毎フレーム位置と不透明度を更新）で表示します。

### 7. 経路計画（`planAxisPath`）の座標の作り直し
1卓4席・キッチン拡張・レジ移動により、テーブル・通路・危険地帯（帯）の座標がすべて
変わったため、`TABLE_BAND_TOP/BOTTOM`・`CORRIDOR_XS`（卓と卓の隙間の安全な縦移動用x座標）を
新しいレイアウトに合わせて再計算しました。考え方自体は4-h節の設計をそのまま踏襲しています
（詳細は下記「動作確認」）。

### 意図的な簡略化・正直な注記
- ウェイターが配膳・注文を取りに行く際の目標地点（`servePoint`）を、客の椅子の位置
  （`seatPoint`）とまったく同じ座標にしています。給仕のたびに客の真横まで来る形になり、
  「向かい合う」ような立ち位置の演出（テーブルを挟んで対面に立つ、等）はまだありません。
- 経路計画（`planAxisPath`）は引き続き「最短距離」を保証しない簡易ルールのため、通路の
  選び方によっては目的地の反対方向へ一度大きく迂回してから向かうケースがあります
  （テーブルに重ならないことを最優先にした結果で、ご依頼どおり最短経路自体は要件外です）。
- 会計金額のポップアップは特定のキャラクターに追従させず、レジという「場所」に固定して
  表示しています（会計直後に客がすぐ`LEAVING`へ歩き出してしまうため、客に追従させると
  ポップアップも一緒に流れていってしまい「レジから飛び出た」感が薄れるための判断です）。

### 動作確認
`node --check`で`assetManifest.js`・`actorFSM.js`・`actorSprite.js`・`sceneV2.js`の構文を
確認した上で、Playwrightで`asset_test.html`を実行し、以下を確認しました。
- コンソールエラー・異常な404（favicon.ico以外）がゼロ。
- **1卓4人がけの実質確認**: 20秒間・1200フレームのサンプリングで、3卓すべてが
  同時に**4人ずつ**（着席系状態の客）で埋まる瞬間が実際に発生することを確認
  （`{"A":4,"B":4,"C":4}`）。
- **斜め移動ゼロ**: 20秒間・1200フレームで、どのキャラクターも1フレーム間にx・yが
  同時に変化したケースが0件。
- **テーブルとの重なりゼロ**: 25秒間・1500フレームで、キャラクターがテーブルの矩形と
  重なったケースが0件（新しい卓・通路座標でも4-h節の衝突回避ルールが機能し続けている
  ことを確認）。
- **コックが厨房から一切出ないこと**: コックのY座標が76〜110の範囲（厨房エリア内）に
  収まり続け、`IDLE`/`WORKING`/`PLACING`以外の状態に遷移しないことを確認。
- **ウェイターが2人体制のフローを一通りこなすこと**: `IDLE → NOTICE_CUSTOMER →
  WALKING_TO_CUSTOMER → TAKING_ORDER → RESTING → WALKING_TO_COUNTER → CARRYING_FOOD →
  SERVING → CASHIER`の全状態への遷移を確認（＝接客・カウンター受け取り・配膳・会計を
  1人のウェイターが一通りこなしている）。
- **料理が卓上の固定点に置かれ続けること**: 同じ客が`EATING`状態の間、料理の描画座標
  （`translate(...)`）が完全に一致し続けることをフレーム単位で確認。
- **会計ポップアップ**: `¥800`のような文字列がレジの位置から生成され、上へ浮かびながら
  消えることをDOM上で確認。
- **性能**: 30秒間・1800フレームで平均**60.0fps**を維持（最大フレーム時間24.9ms）。
  座席数を3→12、スタッフを1→2に増やした後でも速度低下は発生していません。
- スクリーンショット・GIF（`demo_v4.gif`）で、縦長テーブル＋左右2脚ずつの椅子・広がった
  キッチン・コックとウェイターの分業・入口そばのレジと会計ポップアップを確認。

## 4-j. Phase 5・ステージ6「椅子の反転・キッチンの作り込み・客の状態可視化・HUD表示・会計連動」（今回の追加分の詳細）

「①卓の右の椅子を左右反転②キッチンをもっと細かく広く③客が何をしているか分からない・
急に消える④ログの右に日付/時刻/入店数/所持金を表示⑤レジの金額と合計金額を連動」という
5点のフィードバックに対応しました。

### 1. 卓の右側の椅子を左右反転
これまでは左右どちらの椅子も同じ向きの画像をそのまま描画していたため、卓の右側の椅子は
背もたれが内側（卓側）を向いてしまい不自然でした。`renderFurniture()`に`flip`フラグを
追加し、右側の椅子だけ`translate(2*cx,0) scale(-1,1)`という変換（自分自身の中心xを軸に
その場で鏡像化）を掛けて描画するようにしました。パスファインディング側の座標
（`seatPoint`等）は一切変更していないため、見た目だけが反転し当たり判定・経路計画への
影響はありません。

### 2. キッチンをより細かく・広く
これまで冷蔵庫・コンロ・カウンターの3点だけだったキッチンに、`Kitchen_Singles`フォルダ
から新たに3点の実素材を切り出して追加しました。
- 電子レンジ付きキャビネット（`Kitchen_Singles_124.png`）
- 食洗機（`Kitchen_Singles_187.png`）
- ガラス扉のパントリー棚（`Kitchen_Singles_193.png`）

什器点数を3→6に増やしただけでなく、キャンバス幅（`VIEW_W`）を640→760に拡張し、
奥壁沿いに5点（冷蔵庫・コンロ・電子レンジ・食洗機・パントリー棚）を並べた「奥の什器列」と、
コック/ウェイターが料理を受け渡す横長カウンター（幅74→210に拡張）を手前に配置する
2列構成にしました。テーブル3卓・通路・レジ等の座標もすべて右にシフトさせて再計算して
います（`TABLE_XS`・`CORRIDOR_XS`・`WORLD`の各点）。

### 3. 客の状態を頭上アイコンで常時可視化
「客が何をしているか分からない、急に消えたように見える」というご指摘を受け、客の状態
それぞれに対応する頭上アイコンを追加しました（状態が変わるたびに切り替わり、次に状態が
変わるまで表示され続けます）。
- 👀 席を探している（`LOOKING_FOR_SEAT`）
- ⏳ 注文を取りに来てもらうのを待っている（`WAITING_FOR_ORDER`）
- 📝 注文中（`ORDERING`、既存）
- 🍳 料理を待っている（`WAITING_FOR_FOOD`）
- 🍴 食事中（`EATING`）
- 💴 会計中（`PAYING`）
- 👋 退店中（`LEAVING`）——「消えた」のではなく歩いて帰っている最中であることを明示

あわせて、複数人が同時に待っていても重なりにくいよう、入店直後の待機位置のジッター幅を
拡大しました（横±35→±70px、縦±15→±25px）。また、テンポが速すぎて状況を追いにくい
という面もあると考え、客の来店間隔を少し落ち着かせています（0.5〜1.1秒→0.7〜1.4秒）。

### 4. ゲーム内クロック・HUDパネルの新設
まだ既存の経済シミュレーション本体（`core/simulation.js`、週送りのターン制）とは接続して
いないため、`sceneV2.js`側に表示専用の簡易な「ゲーム内クロック」を新設しました。実1秒＝
ゲーム内6分のペースで進み、開店10:00〜閉店22:00の1日が終わると翌日10:00へ、7日ごとに
週間カウントもリセットされます。客が入店するたびに本日/週間の入店数をカウントし、
所持金は初期資金40万円（`PROGRESS.md`5節の設定に合わせた値）からスタートします。
`getHudState()`でこれらを取得できるようにし、`asset_test.html`側にログ（`#status`）の
右にHUDパネルを新設して、日付・時刻・本日/週間入店数・所持金を表示するようにしました。

### 5. 会計金額と所持金の連動
`showPayment()`内で、ポップアップ用のテキストを生成するのと同じタイミングで
`totalMoney += price`を行うようにし、レジから飛び出す金額表示と所持金の増加が完全に
同じ瞬間に起きるようにしました（Playwrightで、ポップアップに「¥800」が表示された
フレームと所持金が+800されたフレームが一致することを確認済み）。

### 意図的な簡略化・正直な注記
- 今回追加したゲーム内クロック・入店数・所持金は、あくまで検証ページ（`asset_test.html`）
  表示用の簡易カウンタです。既存の週送り経済シミュレーション（`core/simulation.js`）とは
  まだ接続していません（本編との統合は今後のステップとして`PROGRESS.md`8節に記載済み）。
- 所持金は会計時の加算のみを実装しており、人件費・仕入れ費用などの支出は今回反映して
  いません（支出込みの収支計算は本編統合時に`simulation.js`側の既存ロジックに委ねる想定）。

### 動作確認
`node --check`で`assetManifest.js`・`sceneV2.js`の構文を確認した上で、Playwrightで
`asset_test.html`を実行し、以下を確認しました。
- コンソールエラー・異常な404（favicon.ico以外）がゼロ。
- 22秒間・1320フレームのサンプリングで、斜め移動0件・テーブルとの重なり0件（新しい
  座標でも4-h/4-i節の衝突回避ルールが機能し続けていることを確認）。
- コックのX座標が75〜125・Y座標が76〜140の範囲（キッチンエリア内）に収まり続け、
  拡張後のレイアウトでも客席側に一切出ないことを確認。
- スクリーンショットで、卓の右側の椅子が左側と鏡合わせに（背もたれが外側を向いて）
  描画されていること、キッチンに5点の什器が奥壁沿いに並んでいること、客の頭上に
  状態に応じたアイコン（⏳🍳🍴💴👋等）が表示され続けることを確認。
- HUDパネルに日付・時刻・本日/週間入店数・所持金が表示され、時刻が実時間の経過に応じて
  正しく進むこと（実測: 実測約38秒でゲーム内約3時間49分経過。設定値の6分/秒に一致）、
  客が入店するたびに入店数がカウントされることを確認。
- 会計ポップアップの金額（例: ¥800、¥850）が表示されたフレームで、所持金がまったく
  同じ額だけ増加することをフレーム単位で確認（例: 400,000→400,800→401,650）。
- **性能**: 30秒間・1800フレームで平均**60.0fps**を維持（最大フレーム時間22.2ms）。
  キャンバス拡張・什器追加・HUD更新を加えた後でも速度低下は発生していません。
- デモGIF（`demo_v5.gif`）で、反転した椅子・作り込まれたキッチン・客の状態アイコン・
  HUDパネルの表示を確認。

## 4-k. Phase 5・ステージ7「ゲームに反映（新PNGエンジンを本編FIELD画面に統合）」（今回の追加分の詳細）

### 何を確認してから着手したか
「ゲームに反映して。」という一言だけの指示は、「単に新エンジンがどこかに存在していれば
よい」のか「本編`index.html`のFIELD画面そのものを新エンジンで動かしたい」のかが曖昧で、
かつ後者の場合でも実装コストの大きく異なる2つの分岐点があったため、実装前にユーザーへ
2点確認した。

1. **統合方式**: 「見た目だけ差し替え（推奨）」と「新エンジンの自律シミュレーションを
   本編の主軸にする（`simulation.js`の週次一括計算を作り直す必要がある大改修）」の
   どちらか → **「見た目だけ差し替え（推奨）」を選択**。
2. **LAYOUT画面の扱い**: 「今回は固定レイアウトのままでよい」と「LAYOUT画面での
   自由配置を新エンジンの描画にも反映したい」のどちらか → **「LAYOUT画面の配置も
   新エンジンに反映したい」を選択**。

この結果、実装方針は「`core/simulation.js`のbeat生成ロジックは一切触らない。FIELD画面の
描画エンジン（`ui/scene.js`）だけを、`asset_test.html`で検証済みのPNGベース描画に
差し替える。ただし、什器・卓・厨房・レジの配置は`asset_test.html`のような固定配置では
なく、プレイヤーがLAYOUT画面で実際に編集した`Game.App.state.layout`から都度組み立てる」
という形に確定した。

### 実装方針：「旧scene.jsの骨格」×「新エンジンの見た目」
旧`ui/scene.js`は最初から`Game.Core.Layout`（グリッド読み取り）・`Game.Core.Pathfind`
（BFS経路探索）の上に作られており、任意のレイアウトに対応する構造は既に持っていた
（`asset_test.html`の新エンジン側は逆に、3卓固定・軸移動限定の座標が直書きされた
検証専用の簡略構造だった）。そのため、「レイアウト非依存」という土台部分は新エンジン側の
コードを持ち込むのではなく、**旧scene.jsの制御構造（グリッド座標・BFS移動・beatごとの
演出ハンドラ・厨房ジョブキュー）をそのまま土台として残し、そこに新エンジンの素材と
アニメーション表現だけを移植する**、という方針を採った。これにより、LAYOUT画面の
自由配置対応（要確認事項2）を新たに設計し直す必要がなくなった。

具体的な差し替え箇所:
- **什器・卓・椅子の描画**: `objectSvg`/`tableSvg`/`chairSvg`によるSVG図形の直接描画を
  廃止し、`Game.Data.FurnitureAssets`のPNG画像を各マスの下辺中央に配置する方式に変更。
  卓の右側にある椅子は`translate(2*cx,0) scale(-1,1)`でその場鏡像化し、ステージ6と同じ
  「両側とも卓の方を向く」見た目にした（LAYOUT画面でプレイヤーが卓をどこに置いても、
  隣接する椅子ごとに動的に左右判定するため、卓の位置が変わっても正しく反転する）。
- **キャラクター描画**: `Game.UI.Sprite.buildCharacterGroup`（パーツ合成の旧SVGキャラ）
  への参照を全廃し、`Game.UI.ActorSprite.build`（LimeZu完成絵PNG、多フレーム歩行/待機
  アニメ付き）に置き換えた。客は`Game.Data.CharacterAssets.randomLook()`でスポーンの
  たびに14種類からランダムな見た目を、コックは"staff_chef"、ウェイター役は"staff_service"
  （黒服）を使う。
- **厨房のワークフロー刷新（本来のご要望への手戻り対応）**: 旧scene.jsは実は「コックが
  自分で調理して自分で客席まで運んで自分でコンロへ戻る」という1人二役の実装になっており、
  もともとのご要望「キッチン用カウンターからウェイターが受け取る形にして」を満たせて
  いなかった（`asset_test.html`の新エンジン側では満たせていた）。今回の統合を機に、
  本編でも正しい2段階の受け渡しに直した：コックは調理後、厨房什器（コンロ優先。無ければ
  任意の厨房カテゴリ什器）の隣接マス＝カウンター地点まで運んで置くだけで、コンロへ戻る。
  そこから「ウェイタープール」（役割が`service`または`register`かつ勤務日数>0のスタッフ、
  居なければ他の出勤スタッフで代用）に属するスタッフが受け取りに来て、客のテーブルまで
  運ぶ。コック・ウェイターの人数はどちらも固定1人ではなく、`Game.App.state.staff`の
  実際の雇用状況分だけスポーンする（コックが複数いる場合は厨房什器の地点を巡回で
  割り当てる）。
- **客の状態の可視化**: ステージ6と同じ思想で、客の状態（席へ移動中／料理待ち／調理待ち／
  食事中／会計中／退店中／諦めて帰る）に応じた頭上アイコン（👀⏳🍳🍴💴👋🚫）を、
  一定時間で自動的に消えるタイマー式ではなく「状態が変わるまで表示され続ける」方式で
  実装した。
- **会計演出**: 会計時に実際の商品価格（`Game.App.state.products`の`currentPrice`）に
  基づいた金額の数字（例: ¥850）がレジから浮かび上がるようにした（旧scene.jsは💴の
  絵文字アイコンのみで、金額そのものは表示していなかった）。

### あえて持ち込まなかったもの：新エンジンの自律タイムアウト処理
`asset_test.html`の新エンジン（`core/actorFSM.js`）は、席を探して一定回数失敗したら
諦める・注文を取りに来てもらえず一定時間待ったら諦める・料理が来ず一定時間待ったら
諦める、といった「キャラクター自身が自発的に判断して帰る」タイムアウト処理を持っている。
これは`asset_test.html`のような自律シミュレーションでは自然な挙動だが、本編は
「`simulation.js`が既に何人が提供され何人が離脱したかを確定させ、それをbeatとして
後から再生しているだけ」という前提（要確認事項1の回答）のため、演出側が独自に
「やっぱりこの客は諦めた」と判断してしまうと、営業ログのテキスト（確定した数値）と
食い違う演出になってしまう。そのため`core/actorFSM.js`は今回の統合には使わず、
本編の客の状態遷移はすべて「beatが指示したタイミングでのみ」進む、タイムアウトの
無い専用の状態管理をscene.js内に新たに書き起こした（席を探す→座る→beatで配膳される
まで待つ、のように、次に何が起きるかを常にbeat側が決める）。

### 実装中に見つけて直したバグ
`processWaiters()`の初期実装で、1tick内に複数件の配膳をさばく`while`ループの中で
`var waiter`/`var ent`等をループの外側（関数スコープ）で使い回していたため、非同期の
`setPath`コールバックが実際に発火する頃には、それらの変数が後続の反復で別の値に
上書きされてしまい、`Cannot read properties of undefined (reading 'home')`という
実行時エラーが発生していた（JSの`var`はループ内で再利用されるための典型的な落とし穴）。
1件の配膳ディスパッチを`dispatchWaiterJob(waiter, job)`という独立した関数呼び出しに
切り出し、そのつど新しい関数スコープを作ることで解消した。

### 動作確認
`node --check`で`js/ui/scene.js`の構文を確認した上で、Playwrightで実際の`index.html`を
以下の手順で検証した：LAYOUT画面でテーブルを1卓追加配置→OFFICE画面で「営業開始」→
FIELD画面の各曜日を自動で「次の日へ進む」しながら数十秒間観察→「結果を見る」まで到達。
- コンソールエラー・ページエラーがゼロ（上記のバグ修正後）。
- LAYOUT画面で追加したテーブルが、FIELD画面の什器としてエラーなく描画されること
  （`#scene-furniture image`の要素数が配置前後で正しく増減）を確認。固定ハードコード
  レイアウトではなく`Game.App.state.layout`から都度組み立てていることの裏付け。
- 実際の営業再生中に、入店（👀→⏳）・行列発生・調理待ち（🍳）・配膳（🍴）・
  待ちきれず離脱・会計（💴のポップアップ＋実際の商品価格の数字）・退店（👋）の
  一連の状態が頭上アイコンとログテキストの両方に矛盾なく現れることを確認。同時に
  最大12人前後の客が画面内で異なる状態（着席中／料理待ち／食事中等）を保ちながら
  進行することも確認した。
- 週の最後まで通しで再生し、結果パネル（売上・利益・提供数・離脱数・評判変化・所持金）
  が正しく表示され、ヘッダー上部の週数・所持金・評判（`simulation.js`が既に確定させた
  実データ）がそのまま反映され続けていること（新エンジン側の独自クロックやカウンターに
  一切上書きされていないこと）を確認した。
- 併せてスクリーンショットで、キッチン什器（冷蔵庫・調理台・コンロ2つ）の配置、
  客席側の卓＋椅子（左右反転済み）、コック・ウェイター役スタッフのPNGキャラクター
  表示を目視確認した。
- `asset_test.html`側（`sceneV2.js`・`actorFSM.js`）は変更しておらず、単体検証ページ
  としては引き続き独立に動作する。

### 今回あえて置いていった簡略化
- カウンター地点（コック→ウェイターの受け渡し場所）は、厨房什器のうち最初に見つかった
  1地点をコック全員・ウェイター全員が共有する形にしている（複数コックが同時に別々の
  カウンターを使い分けるような演出にはなっていない）。
- 家具の当たり判定はこれまで通りマス単位の簡易判定のまま。テーブル・冷蔵庫など縦に
  長いPNG素材は、意図的にマスの下辺を基準に上方向へややはみ出して描画しており
  （見下ろし視点の什器としては一般的な表現）、真上から見ると隣接する奥のマスに
  少し重なって見えることがある。
- 客の見た目（`randomLook`）は入店のたびに毎回ランダムに決まる方式のままで、
  「同じスタッフIDなら常に同じ見た目」という一貫性は客については持たせていない
  （スタッフ側はロール固定の見た目のため常に一貫している）。

## 4-l. Phase 6「開業準備（難易度/業種/店名/初期メニュー）＋日々の未来視システム」（今回追加分の詳細）

### ユーザーからの仕様変更依頼
「最初に、難易度を選ぶ／お店を選ぶ（ラーメン・和食・洋食・デザート）／お店の名前を決める／
最初のメニューを決める、を実施できるようにしてほしい」「未来視システムを、①最初の未来視
＝ゲーム難易度（EASY/NORMAL/HARD）、②日々の未来視＝全員デフォルトで無料の、翌日以降の
抽象的な予兆・傾向を示す情報（具体的な正解は教えない）、の2階層に分けて実装してほしい。
既存のシミュレーション・店舗運営システムは壊さずに自然に組み込むこと」という仕様変更依頼を
受けて実装した。

### 着手前に確認した3点（AskUserQuestionで確認・すべて推奨案を選択）
1. **業種とメニューの関係**: 商品データは元々1業種1品（ラーメン/定食/パスタ/デザート）しか
   無く、「最初のメニューを決める」を実際の選択にするには商品バリエーションが必要 →
   **「業種ごとに商品を追加し、そこから選ばせる」を選択**。これに伴い`products.js`を
   1業種3品×4業種＝12品のカタログに拡張した（4-l節末尾に一覧）。
2. **日々の未来視の頻度**: このゲームは「1週間=1ターン」で、経営判断（価格・仕入れ・シフト等）
   はOFFICE画面で週の始めに1回だけ行い、その後は1週間分が自動再生される構造。「日々の未来視を
   見て日ごとに経営判断する」という仕様とどう折り合いをつけるか →
   **「週次ターンは維持し、週の初めに月〜日7日分の見通しをまとめて表示する」を選択**。
   既存の`core/simulation.js`のターン構造（週1回の意思決定→1週間分のbeat生成）は変更していない。
3. **既存の「未来見通し」🔮スキルとの整理**: スキルポイントを使って習得し、その週に集中する
   （代償あり）と来週いちばん売れそうな商品ヒントが見える`foresight`スキルが既にあり、今回の
   「全員デフォルトで無料の日々の未来視」と役割が重なる →
   **「無料の基本版＋スキルは上位互換として残す」を選択**。`getForesightHint`（スキル側）は
   一切変更しておらず、そのまま数値まで踏み込んだ上位互換のヒントとして共存する。

### 実装内容
- **難易度（`js/data/difficulty.js`、新規）**: EASY/NORMAL/HARDの3段階。それぞれ
  `trafficMult`（客足）・`priceSensitivity`（値上げへの敏感さ）・`ingredientCostMult`（仕入れ
  コスト）・`patienceMult`（客の我慢強さ）・`startMoneyMult`（開業資金）の5つの倍率を持つ。
  **業種による有利不利は一切作らない**（`core/simulation.js`の`generateWeekConditions`で、
  `state.storeType`を一切参照せず`state.difficulty`の倍率だけをイベント効果に乗算で合成）。
- **業種（`js/data/storeTypes.js`、新規）**: ラーメン店(中華)／和食店／洋食店／デザート店の4つ。
  既存の`core/simulation.js`の`CATEGORIES`（中華/和食/洋食/デザート）と1対1対応。
- **商品カタログ拡張（`js/data/products.js`）**: `Game.Data.PRODUCT_CATALOG`（業種→商品リスト、
  計12品）を新設。既存の`Game.Data.PRODUCTS`は全業種のフラット配列として後方互換に残した
  （旧セーブの移行や「業種未指定」時のフォールバックに使用）。各商品は`foodKey`
  （FIELD画面で頭上に出す料理アイコンの素材キー）を自分で持つようにし、`ui/scene.js`の
  `foodKeyForProduct`もハードコードされたマップからstate.products参照に変更した。
  実在する料理アート素材が5種類（ramen/pasta/parfait/curry_rice(teishoku)/hamburger）しか
  無いため、新しい商品の一部は同カテゴリ内の近いart（例：餃子→pasta素材の「皿に丸く盛られた
  料理」を流用）を共有しているが、価格・原価・人気度など数値データは商品ごとに独立している。
- **オンボーディング画面（`js/ui/setup.js`、新規＋`index.html`に`#screen-setup`追加）**:
  「①難易度→②業種→③店名→④初期メニュー」の4ステップウィザード。初回起動時（続きのセーブが
  無い場合）だけ表示され、`#tabs`・`#topbar`は非表示にして通常画面には一切触れられないように
  している。完了すると回答を`Game.Core.State.createInitialState(answers)`に渡して初めて本当の
  ゲーム状態が作られ、OFFICE画面に遷移する。
- **状態管理（`js/core/state.js`）**: `createInitialState(setupAnswers)`が難易度・業種・店名・
  選択商品を反映するようになった（未指定時はNORMAL難易度・全業種商品というフォールバック）。
  開業資金は`400,000円 × 難易度のstartMoneyMult`。`migrate()`で旧セーブ（このセットアップ機能
  より前のセーブ）に`setupComplete:true`・`difficulty:"normal"`・`storeName:"無名食堂"`等を
  補完し、既存セーブを一切壊さないようにした。
- **日々の未来視（`js/core/simulation.js`の`getDailyForesight`、新規）**: 週の始めに
  `generateWeekConditions`と同時に1回だけ生成し`state.currentWeekForesight`に保存する
  （OFFICE画面とFIELD画面の各曜日タブで必ず同じ内容が再掲される＝後から答え合わせのように
  作り直したりしない）。各曜日ごとに4行の抽象的なヒントを出す：
  ①客足の強さ（`conditions.dayTraffic`という新設の曜日別倍率から言葉に丸めて表現。
  週の合計来客数自体は変えず、実際の来客が曜日ごとにその傾向へ寄るよう`runWeek`の来客配分
  ロジックにも反映したため、結果を見て「なるほど」と対応が分かるようになっている）、
  ②注目商品の人気（`conditions.productDemandMult`を重みにした抽選で商品を選び、
  「人気が高まりそう/苦戦しそう」等の言葉に丸める。具体的な%は出さない）、
  ③食材価格の動き（`conditions.ingredientCostMult`から）、④時間帯・客層の空気
  （メカニクスと連動しない純粋な雰囲気フレーバー。ユーザー提示の例
  「夕方から若い客が増える気配があります」と同種のもの）。
- **UI表示**: OFFICE画面に「🔮今週の未来視」パネル（月〜日を一覧表示、`office.js`）、
  FIELD画面に現在の曜日タブに対応する未来視だけを再掲する小さいパネル（`field.js`、
  準備日・結果画面では非表示）、ヘッダーに店名・業種アイコン・難易度バッジ（`#hud-store`）を追加。

### 動作確認（Playwright）
- 初回起動でセットアップ画面が出て`#tabs`/`#topbar`が非表示になること、4ステップを順に
  回答して開業すると選んだ業種の商品だけがOFFICEの仕入れセクションに反映されること、
  難易度による開始資金の差（EASY: 460,000円／HARD: 360,000円、いずれも400,000円×倍率
  通り）、店名・業種アイコン・難易度がヘッダーバッジに反映されることを確認。
- 開業後、月〜日7日分の未来視がOFFICEとFIELDの両方で一致して表示され、選んだ業種以外の
  商品は一切登場しないこと、実際に「営業開始」→1週間再生→結果画面まで、コンソール
  エラー0件・スタッフの重なり0件・途中停止なしで通ることを確認（バグ修正1〜3で直した
  内容が今回の変更後も引き続き機能していることの回帰確認を兼ねる）。
- 旧セーブ（このセットアップ機能追加前の形式を再現したデータ）を読み込んでもセットアップ
  画面を経由せず、NORMAL難易度・「無名食堂」・foodKeyの補完まで含めて正しく移行され、
  そのままプレイを続けられることを確認。
- 「最初からやり直す」ボタンを押すと、通常のリセットだけでなくセットアップ画面まで
  戻ること（難易度・業種・店名・初期メニューを選び直せること）を確認。
- 既存の「破産セーブを読み込むと営業開始ボタンが無反応になる」バグ（前回修正済み）が、
  セットアップ完了済みのセーブに対しては引き続き正しく動作する（セットアップ画面を
  スキップしてGAME OVERオーバーレイが復元表示される）ことも確認。

### 今回あえて置いていった簡略化・今後の検討事項
- 業種を1つに絞ると、他3業種向けの「商品力」系スキル（`specialty_washoku`等）はその
  プレイスルーでは実質使い道が無くなる（自店の商品が全て1カテゴリのため）。スキル自体を
  隠す・差し替える対応はしていない（意図的な割り切り。将来「メニュー変更」特殊行動で
  他業種の商品も扱えるようになれば自然に解消する）。
- 初期メニュー選択後にメニューを追加・入れ替える手段はまだ無い（`office.js`に元々あった
  「メニュー変更は今後のバージョンで追加予定」の注記通り。`PRODUCT_CATALOG`は業種内の
  未選択商品も保持しているので、将来の「メニュー変更」機能はこのカタログからそのまま
  追加できる設計にしてある）。
- 「日々の未来視」の客足・商品人気ヒントは、対応する曜日が来るまで的中率を検証する
  手段が無い（あえて数値を出さない設計のため）。プレイテストで「結果を見て後から
  納得できる」ちょうど良い抽象度になっているか、実際に確認してほしい。
- 店名は開業後に変更する手段が無い（プロトタイプ版の仕様として意図的）。

### 業種ごとの商品カタログ一覧（`js/data/products.js`）
- 中華（ラーメン店）: 醤油ラーメン／味噌ラーメン／餃子
- 和食（和食店）: 焼き魚定食／かつ丼／ざるそば
- 洋食（洋食店）: トマトパスタ／ハンバーガー／オムライス
- デザート（デザート店）: パフェ／ショートケーキ／プリン

## 4-m. バグ修正4・5「FIELD画面の客の二重表示」「椅子に座る位置のずれ」（今回の対応の詳細）

### ユーザーからの報告
「フィールドについて、二重になるバグが直ってません。後、椅子に座る位置がずれています。
プレイライトで動作させて直るまでしっかり確認してください」という報告を受けた。以前の
セッションで「同ロールスタッフの重なり」（コック/ウェイターの待機位置が重なる問題）は
修正済みだったが、それとは別に、実際のプレイでは**新しく来店した客同士**が重なって見える
症状が残っていた、という報告。前回の「日をまたいで動きが固まる」バグの際にコードレビュー
だけで「直った」と報告して再度直っていなかった反省を踏まえ、今回は必ずPlaywrightで実際の
症状を再現してから修正し、修正後も同じ方法で「重なりが実際に無くなったこと」「椅子と客の
座標が一致すること」を数値・スクリーンショットの両方で確認する、という手順を徹底した。

### 二重表示バグの真因（2つ複合していた）
1. **同じbeatで同時入店する客が、入口の全く同じ座標・同じ速度で同じ経路を歩いていた**:
   `handleEnterSeat()`は1つのbeatにつき最大4人（`ANIM_CAP_ENTER`）までその場で
   `spawnCustomerToSeatBeat()`を同期的に呼び出しており、全員が入口セルの中心という
   全く同じピクセル座標から、同じフレームで、同じ`WALK_SPEED`で歩き始めていた。経路の
   分岐点（自分の席）にたどり着くまでは複数人の経路が重なる区間があり、その間は複数の
   客が完全に同一座標を歩き続けるため、実質「1人しかいない」ように見えていた
   （Playwrightで客エンティティの`<g>`のtransform座標をサンプリングし、複数IDが
   ピクセル単位で完全一致したまま数百ms〜1秒近く推移することを直接確認した）。
2. **レジ什器のすぐ隣が入口セルだったため、ウェイターの待機位置プールに入口セル自体が
   紛れ込んでいた**: デフォルトレイアウトはレジを`(1,3)`に配置しており、その隣接歩行可能
   マスの1つがちょうど入口`(0,3)`と一致する。`nearbyDistinctCells()`はレジ什器に隣接する
   歩行可能マスをそのまま待機位置候補に入れていたため、ウェイターが入口のど真ん中に立ち
   続けることがあり、次々と入店してくる客と常に重なって見えていた。

### 修正内容（`js/ui/scene.js`）
- `handleEnterSeat()`/`handleLeaveDisappointed()`で、同じbeat内の2人目以降（`staggerIndex`）
  は、(a) 入口付近で黄金角オフセットにより数px散らした位置に一旦立たせ、(b)
  既存のタイマー/`pendingAction`機構（調理待ち等と同じ仕組み。倍速モードの`speedMult`や
  一時停止にも自動追従し、日替わりでの客の強制消去にも安全に追従する）を使って一人あたり
  260ms程度ずつ歩き出しをずらすようにした（`spawnCustomerToSeatBeat`/`spawnDisappointed`
  の`staggerIndex`引数、`entranceJitter()`、`begin_walk_to_seat`/`begin_walk_to_loiter`
  アクション）。经営シミュレーション側の人数・タイミング（beatそのもの）は一切変更していない
  ——見た目の演出だけをずらしている。
- `nearbyDistinctCells()`が候補に入口セルそのものを含めないように除外を追加し、スタッフの
  待機位置が入口と重なることが無いようにした。

### 椅子ずれバグの真因と修正
什器（椅子・卓等）は`drawFurniture()`で「マスの下辺」を基準に描画される
（`y = セルのY + CELL - 画像の高さ`）のに対し、キャラクターは`ent.x/ent.y`（`cellCenter`＝
マスの中心）にそのまま足元を合わせて描画していたため、椅子より約32px（CELLの半分）上に
浮いた状態で座っているように見えていた。`ent.x/ent.y`自体は経路探索・距離計算・向き判定
などロジック全般で使う論理座標のため変更せず、`<g>`のtransform・吹き出し・皿を描画する
箇所（6箇所）にだけ表示専用の縦オフセット`ENTITY_VISUAL_Y_OFFSET = CELL/2`を追加し、
什器と同じ「マスの下辺」基準にキャラクターの足元を合わせ直した。

### Playwrightでの確認
- 修正前: 客同士の重なり（距離6px未満）が174件発生し、しかも同じペアが複数の連続サンプル
  （数百ms）にわたって完全一致し続ける「持続的な重なり」だった。
- 修正後: 難易度・業種・雇用人数を変えた複数パターンで1週間フル再生を行い、重なり検出を
  連続サンプルで追跡した結果、最大連続一致数は0（＝一瞬すれ違う程度の単発的な近接のみで、
  貼り付いたような重なりは発生しない）。コンソールエラー0件、週の最後まで正常に完走。
- 着席中の客について、吹き出し状態（⏳待機／🍴食事中）で「座っている」客だけを抽出し、
  その足元座標と、対応する椅子スプライトの下辺座標を突き合わせたところ、dx=0・dy=0
  （完全一致）であることを数値・スクリーンショットの両方で確認した。
- 修正版`js/ui/scene.js`をPCへ反映済み（`C:\Clode\スキル経営ゲーム\js\ui\scene.js`、
  サイズ一致を確認済み）。

## 5. バランス調整の現状（重要・プレイテストで一番見てほしい部分）

- 初期資金: 400,000円
- 厨房・接客・レジの処理能力係数（`CAPACITY_MULT`）と週間来客数のベース値
  （`BASE_WEEKLY_TRAFFIC`）は `js/core/simulation.js` の先頭付近で定数化してあります。
- 自動テスト（何も調整せず「営業開始」を連打するだけ）だと、初期スタッフ構成では
  調理担当のキャパシティが来客数に対して不足し、毎週おおよそ -4万〜-7万円の赤字が出て、
  6〜7週目あたりで借金が膨らみゲームオーバーになることを確認しています。
  → これは意図的です（「何も考えずに営業すると詰む」設計）。ただし、仕入れ量を絞る・
  シフトや役割配置を見直す・スキルで需要を先読みする、といった調整で赤字幅が縮む
  ことも確認済みです（仕入れ80%に統一しただけで週次赤字が2〜3割改善）。
- **ここが今回一番フィードバックが欲しいポイントです**: この「難易度の出方」が
  「理不尽」に感じられるか「工夫のしがいがある」と感じられるかは、実際に触ってみないと
  分かりません。きつすぎる／緩すぎると感じたら、上記の定数（`CAPACITY_MULT` や
  `BASE_WEEKLY_TRAFFIC`、`js/data/staff.js` の `wagePerDay`、`js/core/simulation.js` の
  `BANKRUPTCY_LINE` など）を調整するだけで全体の難易度を変えられます。

## 6. 既知の未実装・簡略化している部分（意図的な割り切り）

- **キャラクターの「上向き（後ろ姿）」素材が存在せず、右向きの画像で代用しています**
  （4-g節・4-h節に詳細。全15キャラを対称性スコアで再確認済みで、LimeZu素材そのものに
  後ろ向きの静的ファイルが同梱されていないための制約です。代用先は4-h節でleftから
  rightに変更しました）。
- **従業員（コック帽姿）は正面向きの絵しか無く、左右に歩く場面でも正面向きのまま
  移動します**（4-g節）。より自然な向き替えを優先する場合は"Bouncer"見た目
  （`STAFF_LOOKS.service`）への切り替えも可能です。
- ウェイター服に相当する素材がパック内に見当たらず、コック帽／フォーマル黒服の
  いずれかで代用しています（4-g節）。
- v3でキャラクターの見た目を「パーツ合成」から「役割ごとの完成画像1枚」方式に
  変更したため、髪型・服装を個別にカスタマイズする機能は無くなりました
  （実在しない方向の素材を合成で誤魔化さないための判断。4-g節）。
- レジ担当・その他担当の効果は接客キャパシティへの小さな補正としてのみ実装（独立した
  「レジ待ち」「清掃」等のロジックは無し）。
- 商品開発（新商品を実際に追加するUI）は未実装。スキルの習得だけ可能。
- メニュー変更・施設増強・スタッフ教育・設備増強はボタンなし（今後実装予定の注記のみ）。
- AIによる自然言語スキル生成（要望の8番）は今回のスコープ外（意図的に見送り）。
- 商品の在庫は仕様通り「翌週へ持ち越さない」。
- （Phase 1時点の記述。Phase 2で「テキストログのみ」から「店内を歩き回る空間アニメーション＋
  テキストログ」の併用に拡張しました。詳細は4-b節を参照。）
- キャラクターの見た目は依頼された「実写画像パーツ」ではなく、SVGレイヤー合成による
  簡易ベクターキャラクターです（4-b節で詳しく説明・お詫びしています）。
- FIELD画面のカメラは依頼された「斜め上から見下ろす視点」ではなく、真上から見下ろす2D視点
  のままです（4-c節を参照）。
- モバイル（iPhone）向けの最終パッケージング（PWA化／ネイティブラップ）はまだ行っていません。
  現状はレスポンシブCSS止まりで、まずはブラウザでの面白さ確認を優先しています
  （依頼の「最初はブラウザで動くように」に対応する段階）。
- **（Phase 5・ステージ7で解消）** 旧SVGエンジン（`ui/sprite.js`・`js/data/characterParts.js`）
  は`index.html`から読み込まなくなり、本編は新PNGエンジンの見た目に統一されました
  （ファイル自体は削除せず保管しています。詳細は4-k節）。
- （Phase 5・ステージ7時点）カウンター地点（コック→ウェイターの受け渡し場所）は複数
  コックがいても1箇所を共有する簡略実装です。厨房什器のPNG素材はマス下辺基準で
  描画しているため、背の高い什器は隣接する奥のマスへ軽くはみ出して見えることが
  あります。客の見た目は入店のたびにランダムに決まり、スタッフのような「常に同じ
  外見」の一貫性は持たせていません（4-k節に詳細）。

## 7. 動作確認の方法（このリポジトリを引き継ぐ人向け）

ローカルでPlaywrightによる自動テストを実行済みです。同じ確認を再現する場合:

```bash
cd スキル経営ゲーム
python3 -m http.server 8931 &
# 別ターミナルで
python3 -m pip install playwright && python3 -m playwright install chromium
```

その後、ブラウザで `http://localhost:8931/index.html` を開いて手動確認するか、
Playwrightスクリプトで「購入率ボタンをクリック→営業開始→スキップ→次の週へ」を
繰り返すスモークテストを書いて実行してください（今回のセッションで使ったテストスクリプトの
要点は上記4節・5節の記述に反映済みです）。

## 8. 次のステップ（提案）

0. **【最優先・NEW】4-l節「開業準備＋日々の未来視システム」のユーザーによるレビュー**:
   セットアップ画面の4ステップの流れ・文言、難易度による手応えの差、日々の未来視の
   抽象度（ネタバレしすぎていないか／ヒントとして機能しているか）を実際にプレイして
   確認してもらう。
0.5. 「メニュー変更」特殊行動の実装（4-l節で下地は用意済み。`Game.Data.PRODUCT_CATALOG`
   から選んだ業種の未選択商品を、営業中に追加できるようにする）。
1. **4-h節の修正版（`asset_test.html`・`demo_v3b.gif`）のレビュー**（最優先）:
   コックの二重表示・多フレームアニメ・軸移動・実什器配置・衝突回避・装飾が
   意図通りかご確認ください。4-g節からの残課題（上向き代用・従業員が正面固定・
   ウェイター服の代用）が許容範囲かどうかも合わせてご確認ください。許容できない
   場合は「後ろ向き素材を別途探す/購入する」等の追加対応を検討します。OKであれば
   次段階（LAYOUT画面との統合・既存FIELD画面との統合）に着手します。
1.5. LimeZuのライセンス上、クレジット表記が必要です（購入ページにフォーマットへの
   リンクあり、未確認）。配布前にゲーム内クレジット画面等への記載が必要です。
2. `core/pathfind.js`（BFS経路探索）・LAYOUT画面のグリッド配置と新PNGエンジンを接続し、
   直線移動の簡略化を解消する。
3. ~~新PNGエンジンを既存FIELD画面（`ui/scene.js`/`ui/field.js`、Phase 4のbeat/曜日タブ）に統合し、
   `core/simulation.js`の経済計算と接続する。~~ → **Phase 5・ステージ7で完了**（4-k節）。
   旧SVGエンジン（`sprite.js`/`characterParts.js`）は`index.html`から読み込まなくなりました
   （ファイルは削除せず保管）。
4. **人間によるプレイテスト**: 「設定→営業→結果→次のターン」のループが実際に「店が生きている」
   感触を与えているか、数週間プレイして確認する。
5. フィードバックに応じて `simulation.js` のバランス定数を調整。
6. カメラを「斜め上から見下ろす」疑似3D的な視点にするかどうかの判断（着手する場合は
   座標系・描画順の作り直しが必要な、比較的大きめの変更になります）。
7. 「メニュー変更」「スタッフ教育」など特殊行動の追加実装。
8. ブラウザでの手応えが固まってきたら、iPhone向け（PWA化 or Capacitor等でのネイティブ
   ラップ）の検討に着手する。

## 9. 変更履歴

- 2026-09-20（Phase 1）: 初回プロトタイプ実装。Playwrightで動作確認（30週連続自動プレイで
  エラーゼロ）。バランス調整1回実施（初期資金・賃金・キャパシティ係数・借金/利息システムを
  追加し、「1週目で即詰む」状態を修正）。
- 2026-09-20（Phase 2）: 「店の中・厨房の動きが見えない」というフィードバックを受けて、
  LAYOUT画面（店内レイアウト編集）、パスファインディング、SVGレイヤー合成によるキャラクター
  見た目生成、厨房での調理→配膳→回収と客の入店→着席→注文→食事→退店の空間アニメーションを
  追加。数値計算（simulation.js）は一切変更せず、追加のイベント情報（sceneEvents）のみを
  出力するようにして、演出レイヤー（scene.js）から消費する構成にした。Playwrightで
  既存回帰テスト（test_game.py）・新規テスト（test_scene.py）ともにコンソールエラーゼロで
  合格、15週連続プレイでのDOMリーク無し、レイアウト全消去時のクラッシュ無しを確認。
  「20パーツ×4方向×9カテゴリの実写画像」というご依頼はSVG合成方式に置き換えており、
  4-b節に詳細と経緯を記載。
- 2026-09-20（Phase 3）: 「3等身デフォルメキャラクターが店内で小さな芝居をしている」見た目
  を目指すフィードバックを受けて、キャラクターの頭身比率を作り直し（頭を大きく・胴体を
  丸みのある樽型に）、状態に応じた表情切り替え（待つ／食事／満足／怒り）、連続的な位相に
  基づく歩行バウンド・足の交互運動・食事の口の開閉・トレイを運ぶ動きを実装。役割ごとの
  見た目差（コック帽／レジ担当のバンダナ／エプロン）も追加。店内什器をテーブル＋チェア＋
  皿、コンロのバーナー、冷蔵庫、レジ、入口ドア＋OPENマット等のイラスト風複合図形に刷新し、
  床を木目調に、厨房上部に調理中の湯気エフェクトを追加した。画像生成ソフトの導入を検討した
  が、セッション環境にGPUが無く実用的な速度で動かせないと判断し、コードによる作り込みを
  優先する方針を採った（4-c節に詳細）。カメラは依頼された斜め視点ではなく引き続き真上
  見下ろし視点（4-c節）。Playwrightで既存回帰テストがコンソールエラーゼロで合格することを
  確認。
- 2026-09-20（Phase 4）: 「店舗の様子が全然どうなっているか分からない。曜日ごとに再生可能に
  してほしい。テキストと動きを連動させてほしい。テキストは3行で」というフィードバックを
  受けて、テキストログ（`log`）と空間アニメーション（`sceneEvents`）という別々の配列・別々の
  ペースで再生されていた仕組みを、`beats`という単一の時系列データ（テキスト＋演出アクション＋
  曜日タグを1つのオブジェクトにまとめたもの）に統合。`simulation.js`はテキストと同時に実行
  すべき演出アクションを`beats`に埋め込むだけに変更（数値計算ロジックは無変更。シード固定の
  新旧比較テストで数値が完全一致することを確認済み）。`scene.js`は自律的に客グループを間引き
  再生していた仕組みを撤去し、`playBeat(action)`を渡されたら即座にその場で実行するだけの
  受動的エンジンに変更。`field.js`を全面的に書き直し、beatを曜日（準備／月〜日／結果）ごとに
  グループ化して曜日タブUIを新設、1つの曜日を再生し終えると自動一時停止して「次の日へ進む」
  ボタンに切り替わる方式にした。ログ表示も全文スクロールから直近3行のみのローリング表示に
  変更。`state.js`のセーブデータ形式（`lastWeekLog`/`lastWeekScenes`→`lastWeekBeats`）も
  合わせて移行し、旧形式のセーブを読み込んでもエラーにならないことを確認。Playwrightで
  「テキスト表示と同時にDOM上のキャラクター数が実際に増える」「曜日タブへの自由なジャンプ」
  「結果への直接スキップ」をコンソールエラーゼロで確認済み（4-d節に詳細）。
- 2026-09-20（Phase 5・ステージ1）: ユーザー提供の`chibi_restaurant_assets.zip`（実PNG素材、
  characters/furniture/food/ingredients/ui、9カテゴリ×4方向のキャラクターパーツを含む）を
  正式な素材として採用。「画像生成や仮素材での代用はせず、実際にPNGを読み込んで組み合わせる」
  「ドラクエ的なマス目移動ではなく、状態を持って自律的に動くキャラクター」というご依頼を受け、
  `js/data/assetManifest.js`（画像パス管理）・`js/core/actorFSM.js`（Customer 10状態・Staff
  10状態の純粋なFSM）・`js/ui/actorSprite.js`（PNGレイヤー合成描画）・`js/ui/sceneV2.js`
  （配置・移動・担当割り当て）を新設。まず「客1人・スタッフ1人・テーブル1・椅子1・厨房1・
  料理1種」で入店〜退店の1ループを独立した検証ページ（`asset_test.html`）に実装し、Playwrightで
  全20状態への遷移・ループの継続・コンソールエラーゼロを確認した。既存のOFFICE/LAYOUT/FIELD
  本編（旧SVGキャラクターエンジン）はまだ変更しておらず、次段階で複数客対応・LAYOUT/FIELDとの
  統合を予定（4-e節に詳細。不足素材の一覧も同節に記載）。
- 2026-09-21（Phase 5・ステージ2）: 「動きがもっさりしている、高速化して次へ進めて」という
  フィードバックを受け、`actorFSM.js`に`PACE`倍率（0.28）を導入して全ての待ち時間を一括短縮、
  歩行速度・アニメーション周期も引き上げた。続けて客1人・スタッフ1人・テーブル1卓の固定構成
  から、テーブル3卓・客がランダム間隔で自動来店・最大6人まで同時進行する構成へ拡張し、
  「満席で座れない」「注文/配膳が来ない」場合に既存10状態のまま（新規状態は追加せず）
  `LEAVING`へ諦めて帰る経路を追加してボトルネックを可視化した（ご依頼9・10番）。この過程で
  「客が会計のまま退店しない」バグ（レジ担当への割り当てがスタッフの状態を"IDLE"のまま
  変えずに歩かせていたため、別の客へ二重割り当てされていた）を発見・修正。さらにユーザー
  提供の`chibi_restaurant_assets_v2.zip`を正式素材として採用し、キャラクター素材（skin/ears/
  hair/clothes/pants/shoes/eyes/nose/mouth）・food・ingredients・ui・furnitureの大半を差し替え
  （`assetManifest.js`のみ修正、レイヤー合成ロジックは無修正）。`furniture/entrance.png`が
  v2に同梱されていないため、v1のファイルを暫定流用していることを明示。Playwrightで60秒間の
  実行を行い、34人分のユニーク客IDが入れ替わり続けること・コンソールエラー/異常な404が
  ゼロであることを確認した（4-f節に詳細）。
- 2026-09-21（Phase 5・ステージ3）: 添付の参考画像（陰影・役職差のある画風）レベルを
  求める「根本から考え直したい」というフィードバックを受け、Universal LPC
  Spritesheet Character Generatorを検証したが頭身比率が本作の3頭身デフォルメと
  合わず不採用（実測比較画像で確認）。代わりにLimeZu「Modern Interiors」を購入し、
  同梱の旧フォーマット「Single_Characters_Legacy」（Adam/Bob等20体の完成済み
  キャラ）を採用。ピクセル単位の解析で、この素材には「上向き（後ろ姿）」の歩行
  画像が一切存在しないことが判明し、ユーザー承認のうえ「upはleftの画像で代用する」
  形にした（本物の後ろ向き素材が届き次第、`assetManifest.js`の
  `UP_DIRECTION_FALLBACK`だけ直せばよい作り）。あわせて、キャラクター描画を
  「パーツ合成」から「役割ごとの完成画像1枚」方式に全面変更（`assetManifest.js`・
  `actorSprite.js`）。客14種のランダム見た目、コック帽の従業員（"Chef_Alex"、ただし
  正面専用のため左右移動時も正面向きのまま）、フォーマルな黒服の予備見た目
  （"Bouncer"、こちらは方向あり）を用意。ドット絵が滲まないよう
  `image-rendering: pixelated`をCSSに追加。Playwrightで60秒間の実行を行い、
  33人分のユニーク客IDが入れ替わり続けること・新規のコンソールエラーが無いことを
  確認した（4-g節に詳細）。
- 2026-09-21（Phase 5・ステージ4）: 「①コックが二人で動いている②上向き素材の有無を
  全キャラ再確認③歩行/停止アニメを実装④斜め移動を禁止⑤テーブル・椅子を素材から配置
  ⑥テーブルに重ならない経路移動⑦店内装飾も素材から設定⑧高速動作も担保」という8点の
  フィードバックに対応。コックの左右向きが2コマ混在するバグを正面向きコマの単純コピーに
  修正、全15キャラの対称性スコアを再計測して後ろ向き素材が無いことを再確認した上で
  代用先をleftからrightに変更、方向×動作種別ごとの多フレームアニメーション
  （`characterFramePaths`/`frameIndexFromPhase`）を実装、`planAxisPath()`による軸移動
  のみの経路計画（テーブルを回避する通路方式）を新設、什器一式をLimeZu実素材
  （`assets/furniture_v3/`）に差し替え、壁の額縁・床のラグ等の装飾も追加した。
  Playwrightで斜め移動0件・テーブルへの重なり0件・平均60.0fps（32人同時出入りの負荷下）
  をフレーム単位で確認（4-h節に詳細）。
- 2026-09-21（Phase 5・ステージ5）: 「テーブルを縦長に」「椅子は左右2脚ずつ計4脚」
  「料理をテーブルに置く」「キッチンを広げてカウンター経由でウェイターが受け取る」
  「ウェイターは黒服の人でよい」「レジを入口近くに置き会計金額をポップアップさせる」
  という6点のフィードバックに対応。椅子の配置と厨房動線について「見た目だけの変更か
  実質仕様変更か」を`AskUserQuestion`で確認し、両方とも実質変更（1卓最大4人同時着席／
  コックとウェイターの2人体制）を選んでいただいた上で実装。テーブル画像は16×16ネイティブ
  素材の板目部分をタイル状に継ぎ足す手法で縦長化（48×90px、歪みなし）、`SEATS`を3席→
  12席（3卓×4席、左右2脚ずつ）に拡張して実際に1卓4人同時着席できるようにし、料理は
  客について回らず座席ごとの卓上固定点（`dishPoint`）に置く方式に変更した。
  `core/actorFSM.js`を`CookState`/`WaiterState`という2つの独立した状態遷移表に分割し、
  「厨房の注文キュー」「カウンターの受け取り待ちキュー」を介して疎結合に協調する
  コック（コンロ専任）/ウェイター（接客・受け取り・配膳・会計）の2人体制に刷新。
  ウェイターの見た目は既存のフォーマルな黒服"Bouncer"を転用。レジを入口そばに移設し、
  会計完了時に金額（`js/data/products.js`のbasePrice由来）がレジから浮かび上がる
  ポップアップ演出を追加した。Playwrightで、3卓すべてが同時に4人ずつ埋まること・
  斜め移動0件・テーブルとの重なり0件・コックが厨房から一切出ないこと・ウェイターが
  接客〜会計の全状態を一通りこなすこと・料理が卓上の固定点から動かないこと・会計
  ポップアップの表示・平均60.0fps（座席3→12・スタッフ1→2の負荷増後も速度低下なし）を
  それぞれ確認した（4-i節に詳細）。
- 2026-09-21（Phase 5・ステージ6）: 「①卓の右の椅子を左右反転②キッチンをもっと細かく
  広く③客が何をしているか分からない・急に消える④日付/時刻/入店数/所持金を表示
  ⑤レジの金額と合計金額を連動」という5点のフィードバックに対応。右側の椅子は
  `translate(2*cx,0) scale(-1,1)`によるその場での鏡像化で反転させ、キッチンには
  電子レンジ・食洗機・パントリー棚を追加（什器3→6点）した上でキャンバス幅を640→760に
  拡張して占有面積も広げた。客の状態（席を探す／注文待ち／料理待ち／食事中／会計中／
  退店中）ごとに頭上アイコンを常時表示するようにし、「急に消える」ように見える問題に
  対応。ゲーム内クロック（実1秒＝ゲーム内6分、開店10:00〜閉店22:00）と本日/週間入店数・
  所持金（初期40万円）を管理する`getHudState()`を新設し、`asset_test.html`にHUDパネルを
  追加。会計ポップアップの金額表示と所持金の加算を同一フレームで同期させた。Playwrightで
  斜め移動0件・テーブル重なり0件・コックが厨房エリア内に収まり続けること・会計ポップアップ
  と所持金増加額が完全一致すること・平均60.0fpsを確認した（4-j節に詳細）。
- 2026-09-21（Phase 5・ステージ7）: 「ゲームに反映して。」という指示を受け、統合方式
  （見た目だけ差し替え／新エンジンを主軸にする）とLAYOUT画面の扱い（固定でよい／
  自由配置を反映したい）の2点をユーザーに確認した上で（それぞれ「見た目だけ差し替え」
  「自由配置を反映したい」を選択）、これまで`asset_test.html`という単体検証ページの中に
  閉じていた新PNGエンジンを、本編`index.html`のFIELD画面（`js/ui/scene.js`）に統合した。
  `core/simulation.js`のbeat生成ロジックは一切変更せず、旧scene.jsが元々持っていた
  グリッド・BFS経路探索ベースの「任意レイアウト対応」という骨格をそのまま土台にして、
  什器・卓・椅子・キャラクターの描画だけをPNG素材ベース（`assetManifest.js`/
  `actorSprite.js`）に全面差し替えした。あわせて、旧scene.jsでは満たせていなかった
  「キッチン用カウンターからウェイターが受け取る」という当初のご要望通りの2段階受け渡し
  （コックが調理してカウンターに置く→ウェイター役が取りに来て配膳する）に直し、
  コック／ウェイターの人数も固定1+1ではなく実際の雇用スタッフ数に対応させた。客の状態に
  応じた頭上アイコンの常時表示、実商品価格に基づく会計ポップアップも本編に反映。
  実装中に見つけた`processWaiters()`のループ変数クロージャバグ（同一tick内で複数件を
  さばくと非同期コールバックが後続反復の変数を参照してしまい`undefined`エラーになる
  不具合）も修正した。`index.html`が読み込むスクリプトから旧SVGエンジン
  （`ui/sprite.js`/`js/data/characterParts.js`）を外し、新規に`js/data/assetManifest.js`/
  `js/ui/actorSprite.js`を追加。Playwrightで、LAYOUT画面で追加したテーブルがFIELD画面の
  什器として動的に反映されること・実際の週次営業を最後まで再生してコンソールエラー
  ゼロで結果画面まで到達すること・入店/待機/調理/配膳/会計/退店の一連の状態アイコンと
  ログテキストが矛盾なく対応することを確認した（4-k節に詳細、`demo_v6.gif`参照）。
- 2026-09-21（Phase 5・ステージ7 バグ修正）: ユーザーから「営業開始ボタンを押しても
  何も動きがない」と報告を受け調査。原因は今回の描画エンジン差し替えとは無関係な、
  以前から存在していた不具合だった：`state.gameOver`が`true`のままlocalStorageに
  保存されたセーブ（所持金が破産ライン-100,000円を大きく下回った状態）を再度読み込むと、
  GAME OVERオーバーレイは営業終了直後にしか表示されない仕組みだったため再表示されず、
  「営業開始」ボタンは`wireStartButton`内の`if (self.state.gameOver) return;`により
  何の反応もなく静かに無視され続けていた（Playwrightで`state.gameOver=true`のセーブを
  再現し、ボタンクリック後も画面が切り替わらないことを確認・再現）。`js/main.js`の
  `App.init()`に、読み込んだセーブが`gameOver`済みであれば起動直後にGAME OVER
  オーバーレイを復元表示する処理を追加して修正。修正後、再現テストで
  オーバーレイの再表示・「最初からやり直す」での正常なリセット・リセット後の
  営業開始が正しく動作することを確認した。
- 2026-09-21（Phase 5・ステージ7 バグ修正 その2）: ユーザーから「コックが2重に表示される
  （コック以外でも起きる）」「歩いているとき見ている方向が同じでない」「フィールドの動きが
  毎日ある程度まで進むと止まってしまう」の3点を報告を受け調査・修正した。
  1点目・2重表示：`js/data/staff.js`の既定スタッフは調理ロール2名・接客/レジロール2名が
  同時に稼働するため、コック2人・ウェイター2人が画面に出ること自体は仕様通りだったが、
  `scene.js`の`setupStaffPools()`が同ロールの待機位置（home）を「コンロの隣接マスを
  人数で使い回す」「ウェイターは全員レジの隣接マス1点に固定」という実装になっており、
  什器の隣接歩行可能マスが人数分無いレイアウト（既定レイアウトのレジは1マスのみ）では
  複数人が完全に同じピクセル座標に重なって描画され、「1人しかいないのに2重に見える」
  状態になっていた。什器の周囲の歩行可能マスをできるだけ広く集めて人数分の待機位置候補
  プールを作る`nearbyDistinctCells()`と、そのプールから重ならない位置を人数分割り当て、
  候補が不足する場合のみ円状のピクセルオフセット（jitter）でフォールバックする
  `distinctPositions()`を新設し、コック／ウェイターの持ち場が重ならないようにした。
  2点目・向きの不一致と3点目・途中で止まる：`startDay()`が曜日境目でスタッフプールの
  `busy`フラグだけをリセットしており、調理中／配膳中など「作業の途中」だったスタッフ
  エンティティ本体の`path`/`onArrive`/`timer`/`pendingAction`/`_job`/`_cook`/
  `holdingIcon`が古いまま残っていた。これが次の日の新しいジョブ割り当てと衝突し、
  古い移動経路が後から発火して不自然な向き・停止したような見た目につながっていた
  可能性が高いと判断し、`resetFixtureEntity()`を新設して日が変わるたびに持ち場スタッフの
  進行中の状態を完全にリセットし、位置も持ち場へスナップし直す（`snapToHome()`）ように
  修正した。あわせて、コック／ウェイターが仕事を終えて持ち場へ戻った際も同じく
  `snapToHome()`で重ならない位置に戻るようにした。
  Playwrightで通常営業1週間（準備日〜結果画面まで）を通しで再生し、
  同ロールの2体が3px未満まで接近するケースが「毎フレーム持続する重なり」としては
  発生しないこと（すれ違いによる単発フレームの一瞬の接近が数回あった程度）、
  日付タブが月→火→水→木→金→土→日→結果まで毎回進み途中で止まらないこと、
  コンソールエラーが0件であることを確認した。修正版`scene.js`をPCへ反映済み。
- 2026-09-21（Phase 5・ステージ7 バグ修正3：「途中で止まる」の本当の原因）: 上記バグ修正2の
  「向きの不一致・途中で止まる」への対処（`startDay()`のエンティティ状態リセット）を
  ユーザーに再確認してもらったところ、「途中で止まる」症状は直っていないとの報告を受けた。
  Playwrightで実際のUI操作（曜日ごとに再生ボタンを押す→その日のログ表示が最後まで進んだら
  次の日へ、という通常のプレイフロー）を忠実に再現し、ログ表示が終わった直後の数秒間、
  シーン内のキャラクターの座標が本当に変化し続けているかをフレームごとに記録して調べた
  ところ、真因が判明した：`field.js`の`dayFinished()`（その日のbeat＝ログ行を全て
  表示し終えたタイミングで呼ばれる）が`Game.UI.Scene.pause()`を呼び、シーン側の
  `requestAnimationFrame`ループそのものを止めていた。ところがbeatの表示間隔
  （アクション系で約950ms）は、実際のキャラクターの動き（入店→着席→調理待ち→調理
  （900ms）→配膳→食事（1500ms）→会計→退店、といった一連の動作）よりずっと短いため、
  その日のログが最後まで表示され終わった時点でも、まだ画面上には歩いている途中・食事中
  などの客が十数人残っていた。そこでシーン全体のアニメーションループが完全に停止して
  しまい、「毎日ある程度まで進んだところで動きが固まって見える」という報告の通りの
  状態になっていた（次の日へ進むボタンを押すと`startDay()`が客を強制的に消すため、
  一見「日が変わって再開する」ように見えるが、実際にはただ全消去されていただけだった）。
  修正は、`dayFinished()`から`Game.UI.Scene.pause()`の呼び出しを削除するだけ。
  ログの表示（`field.js`側の次beatのスケジューリング）はその日の分が終わった時点で
  止まるが、シーン側のアニメーションループはそのまま動き続けるようにし、残っている
  客が調理・配膳・食事・会計・退店を最後まで自然にやり遂げられるようにした
  （ユーザーが明示的に一時停止ボタンを押した場合や結果までスキップした場合は、
  従来通りシーンも止まる＝挙動は変えていない）。
  Playwrightで、（1）各曜日のログ表示が終わった直後の数秒間、実際に客の座標が
  動き続けていること（従来はここで完全に静止していた）、（2）倍速モードで1週間を
  通しで再生しても月→火→水→木→金→土→日→結果まで正しく進むこと、（3）同ロール
  スタッフの重なりが起きないこと（バグ修正2の効果が引き続き有効なこと）、
  （4）コンソールエラー0件、を確認した。修正版`field.js`をPCへ反映済み。
- 2026-09-21（Phase 6：開業準備＋日々の未来視システム）: ユーザーから「最初に難易度／業種
  （ラーメン・和食・洋食・デザート）／店名／初期メニューを決められるようにしてほしい」
  「未来視を①最初の未来視＝難易度（EASY/NORMAL/HARD）、②日々の未来視＝全員無料の抽象的な
  予兆情報、の2階層に分けて実装してほしい」という仕様変更を受けて実装（詳細は4-l節）。
  着手前にAskUserQuestionで3点確認（業種ごとに商品を追加してそこから選ばせる／週次ターンは
  維持し週の初めに7日分の見通しをまとめ表示／日々の未来視は無料の基本版・既存の「未来見通し」
  スキルは上位互換として残す、をいずれも選択）。難易度（`js/data/difficulty.js`）・業種
  （`js/data/storeTypes.js`）を新設し、商品データ（`js/data/products.js`）を1業種1品から
  1業種3品×4業種＝12品のカタログに拡張。開業前セットアップ画面（`js/ui/setup.js`、4ステップ
  ウィザード）を新設し、`js/core/state.js`の`createInitialState()`がその回答（難易度・業種・
  店名・初期メニュー）を受け取れるように変更、`js/core/simulation.js`に難易度倍率の合成・
  曜日別客足のばらつき（dayTraffic、実際の来客配分にも反映）・「日々の未来視」テキスト生成
  （getDailyForesight）を追加した。OFFICE画面・FIELD画面（各曜日タブ）に🔮未来視パネルを
  追加し、両者が同じ内容（週の始めに1回だけ生成）を表示するようにした。旧セーブは
  `migrate()`でNORMAL難易度・仮店名を補完し壊れないようにし、「最初からやり直す」は
  セットアップ画面まで戻るようにした。Playwrightで、セットアップ完了後の商品/所持金/
  HUDバッジの反映、月〜日の未来視がOFFICE/FIELD間で一致すること、選んだ業種以外の商品が
  一切登場しないこと、旧セーブの正常な移行、「最初からやり直す」でのセットアップ再表示、
  破産セーブ読み込み時の既存修正が引き続き機能すること、通常の1週間再生（コンソール
  エラー0件・スタッフ重なり0件・途中停止なし＝直近2件のバグ修正の回帰確認を兼ねる）を
  それぞれ確認した。変更したファイル一式をPCへ反映済み。
- 2026-09-21（バグ修正4・5：FIELD画面の客の二重表示／椅子に座る位置のずれ）: 「二重になる
  バグが直ってません。椅子に座る位置がずれています。プレイライトで動作させて直るまで
  しっかり確認してください」という報告を受けて対応（詳細は4-m節）。今回は最初から
  Playwrightで実際の症状（客エンティティの座標サンプリング）を直接再現することから着手し、
  (1)同じbeatで同時入店する客が入口の同一座標・同一経路を辿るため重なって見える、
  (2)レジの隣接マスに入口セルが含まれてしまい、ウェイターの待機位置が入口と重なることが
  ある、という2つの真因を特定した。`js/ui/scene.js`に、同一beat内2人目以降の入店/離脱を
  入口付近でわずかに散らしつつ既存のタイマー機構で少しずつ歩き出しをずらす仕組み
  （経営シミュレーション側の人数・タイミングには一切手を加えていない）と、スタッフの
  待機位置候補から入口セルを除外する修正を追加した。あわせて、什器（椅子等）が「マスの
  下辺」基準で描画されているのに対しキャラクターが「マスの中心」基準で描画されていたため
  椅子より浮いて見えていた問題を、表示専用の縦オフセットを追加して修正した（ロジック用の
  座標は変更していない）。Playwrightで、修正前は174件・持続的だった重なりが、修正後は
  複数パターンの1週間フル再生で最大連続一致0件（単発のすれ違いのみ）まで解消したこと、
  着席中の客の足元座標と椅子の座標がdx=0・dy=0で完全一致すること、コンソールエラー0件で
  週が最後まで正常に完走することを、それぞれ数値とスクリーンショットで確認した。
  修正版`js/ui/scene.js`をPCへ反映済み（サイズ一致確認済み）。
- 2026-09-21（バグ修正6・7・8：「消えるバグ」と二重表示バグの再調査・修正）: 上記バグ修正4・5
  をユーザーに確認してもらったところ、「消えるバグです（『けるの』は『消える』の誤変換
  だった）。2重になるバグも治ってません。もっとしっかり見てください。1週間通して一回も
  発生しないようになるまで修正継続してください。」という、より厳しい再報告を受けた。
  今回は自分が直前に入れた修正自体を疑うところから再調査を始め、合計3つの原因（うち2つは
  バグ修正5で新設したスタッガー機構が引き起こした新規リグレッション、1つはそれとは無関係の
  既存の潜在バグで、これが「消える」の本当の正体だった）を特定した。
  ・バグ修正6（スタッガー待機中の経路上書きレース）：バグ修正5で追加した「入店/離脱を
  タイマーで少しずつずらして歩き出す」仕組みは、客がまだ歩き出す前の待機時間中に
  `setPath()`で別の行き先（例：途中でレジへ呼ばれる等）を割り当てられると、待機時間経過後に
  古い`_beginWalk`が遅れて発火し、新しい経路とonArriveコールバックを黙って上書きしてしまう
  レースコンディションがあった。`setPath()`の先頭で`timer`/`pendingAction`/`_beginWalk`を
  必ずクリアするよう修正。
  ・バグ修正7（退店時のレジ号先解放タイミング）：`sendToRegisterThenExit`/`sendToExit`が、
  客が「退店を決めた瞬間」に座席を即座に解放（`freeSeat`）していたため、まだ画面上を歩いて
  会計・退店している最中の客の席に、新しい客が向かって歩き出してしまい、2体が同じ席の上で
  重なる／片方が隠れて見えなくなるケースがあった（バグ修正5より前から存在した潜在バグ）。
  座席の解放を実際にエンティティが画面から消える`despawnEntity()`時点まで遅らせるよう修正。
  ・バグ修正8（「消えるバグ」の本当の根本原因）：`field.js`の`startDay()`が、日付が変わる
  たびに調理中・食事中・会計中など「まだサービス継続中」の客エンティティも含めて全員を
  問答無用・無演出で即座に消去していた。バグ修正3で「その日のログ表示が終わってもシーンの
  アニメーションは止めない」ようにした結果、「次の日へ進む」ボタンはログ表示終了後すぐに
  押せる状態になるが、実際の客はまだ調理待ち・食事中であることが多く、そこでボタンを
  押す（あるいは自動プレイが押す）と、画面に見えている客がその場で瞬時に消えてしまう
  ―― これがユーザーの言う「消えるバグ」の実体だったと判断した。対処として、
  `Game.UI.Scene`に`hasActiveCustomers()`を新設し、`field.js`の`nextDay()`を、まだ客が
  残っている間は即座に日を進めずに待機する仕組みに変更した：残っている場合は
  「⏳ 退店をお待ちください…」と表示してボタンを無効化し、後片付け用に一時的にシーン速度を
  8倍まで上げて（経済計算には一切影響しない、見た目の待ち時間短縮のみ）残った客が退店し
  きるのを180msごとに監視、最大15秒の安全タイムアウト付きで自動的に次の日へ進めるように
  した。これにより、客が画面上に見えている状態で消えることは原理的に起きなくなった。
  検証は、Playwrightで難易度（EASY/NORMAL/HARD）・業種（ラーメン/和食/洋食/デザート）・
  雇用人数（0〜4人）を変えた5パターンについて、それぞれ1週間フル通し（倍速モード、
  最大150秒/パターン）で(1)エンティティ同士の重なりが「持続的な重なり」として起きないか
  （最大連続一致数）、(2)エンティティが入口から離れた場所で突然消えていないか（単なる
  サンプリング間隔の粗さによる見かけ上の消失ではなく、直前の軌跡が入口へ向かって収束
  していたかどうかを別途判定し、本当に不自然な消失だけを抽出）、(3)コンソールエラー、
  の3点を自動検出するテストハーネスを新規作成して実行した。結果、5パターン全てで週が
  正常に完走、コンソールエラー0件、持続的な重なり（3フレーム以上連続）は発生せず、
  「本当に不自然な消失」は5パターン合計0件（検出された消失候補は全て、直前の軌跡が
  入口へ向かって収束していく正常な退店の見落としであることを確認済み）という結果を得た。
  修正版`js/ui/scene.js`・`js/ui/field.js`をPCへ反映済み（サイズ一致確認済み）。
  なお今回は「1週間通して一回も発生しない」という基準に対してPlaywrightの自動検証で
  ゼロ件を確認した段階であり、実際のブラウザでの操作感（特に「⏳ 退店をお待ちください…」
  の間の待ち時間が長すぎないか）はユーザー自身の確認をお願いしたい。
- 2026-09-21（バグ修正9・10：「まだ直っていない」の再調査 — 前回の修正の抜け穴と、
  スクリーンショットで実際に確認した重なりの再発）: バグ修正6〜8をユーザーに確認して
  もらったところ、「消えるバグです。2重になるバグも治ってません。もっとしっかり見て
  ください。進むときに前を向いたまま進む仕様もデグってます。」という3度目の報告を
  受けた。今回は自分の修正を疑うだけでなく、実際にPlaywrightでスクリーンショットを
  撮って目で確認する（統計的な自動検出だけに頼らない）ことを徹底し、2つの具体的な
  抜け穴を発見・修正した。
  ・バグ修正9（「消える」の再修正・本丸）：バグ修正8では「▶ 次の日へ進む」ボタン
  （nextDay経由）だけを、客が残っている間は待たせるようにしていた。しかし
  `field.js`のコードを読み直したところ、曜日タブを直接クリックする操作・「◀ 前の日」
  ボタンは、どちらも同じ`jumpToDay()`（＝`Scene.startDay()`で客を問答無用に即座に
  消す処理）を経由するにもかかわらず、無防備なままだったことが判明した。曜日タブの
  クリックはごく普通の操作であり、「しょっちゅう消える」という再報告の主因はこちら
  だったと判断した。日付・曜日を切り替えるすべての入り口（曜日タブ・前の日・次の日）
  を、共通の`requestJumpToDay()`を経由させることで統一的に保護し、さらに「同じ日を
  もう一度指定したら何もしない」（無意味な再生成・消失の防止）を追加した。あわせて、
  再生中の日でもこれらのボタンを押せてしまう点も見直し、日付切り替えを要求した瞬間に
  その日の残りbeatの予約（field.js側のsetTimeoutループ）を必ず止めるようにした
  （Scene側のアニメーションは止めない＝バグ修正3の方針は維持）。これを怠ると、
  「客がいなくなるまで待つ」フェーズ中も裏で新しい客がbeatにより送り込まれ続けてしまい、
  待っても待っても客がいなくならない／後片付け用の高速倍率中に次々入店してきて
  再び重なる、という2次的な不具合を引き起こしていた（これはバグ修正8自体が生んでいた
  新しい抜け穴だった）。
  ・バグ修正10（「2重になる」の再修正・実際の重なりをスクリーンショットで確認）：
  Playwrightで実際に週明け（月曜）の一斉来店シーンをスクリーンショットで確認したところ、
  入口付近で複数人の客が実際にほぼ完全に重なって表示される瞬間を目視で確認した
  （自動検出の統計値だけでは見えていなかった）。原因は、バグ修正6で入れた入口での
  ジッター（散らし）が、完全に独立したランダムな半径（最大9px程度）だったため、
  (1)たまたま2人以上のオフセットが偶然近い値になる確率がゼロではなく、
  (2)そもそも半径がキャラクタースプライトの見た目の幅（約45px）よりずっと小さく、
  「重ならない」オフセットが選ばれても視覚的にはほぼ重なって見えていた、という2点。
  さらに、staggerIndexはenter_seat beat 1件ごとに0から数え直される値だったため、
  倍速時などbeatの間隔がENTER_STAGGER_MS（260ms）より短くなる場面では、別々のbeat
  由来の客同士のオフセットが無関係に計算され、たまたま入口の同じあたりに居合わせる
  こともあった。対処として、beatをまたいで単調増加する通し番号を使い、黄金角×
  ANIM_CAP_ENTERと同数（4段階）の半径（16〜46px）で位置を割り振るようにし、
  実際に隣接する通し番号同士の距離が常にスプライト幅以上になるよう計算・スクリーン
  ショットの両方で確認した。修正後は、週明けの一斉来店のような「同時に3〜4人が入口に
  居合わせる」場面でも、各人が視覚的に区別できる程度まで分離されることを確認した
  （完全に隙間なく並ぶわけではないが、以前のような「1人に重なって見える」状態は解消）。
  ・あわせて、「進むときに前を向いたまま進む」という報告を受けて`scene.js`の
  `updateEntity()`の移動ロジックも見直した。従来は1フレームで目的地にぴったり到達する
  （オーバーシュートする）場合、directionを更新せずにその場でスナップしていたため、
  倍速時など1フレームの移動量が大きい場面ではdirectionが更新されない分岐ばかりが
  選ばれ続ける可能性が理論上あった。dist<=stepでスナップする場合も含め必ず
  そのセグメントの向きをdirectionへ反映するよう修正し、あわせて1フレームで複数マス分
  移動する場合にその場で複数区間をまとめて消化するようにした（後片付け用の8倍速時の
  不自然な停止・再開の抑制も兼ねる）。ただし、実際にPlaywrightで「移動方向とスプライトの
  向きが一致しているか」を統計的に検証したところ、修正前・修正後ともに大きな系統的
  不一致（＝常に正面を向いたまま進むような状態）は確認できず、残っていた数%程度の
  不一致は「席に座る瞬間・料理を受け取る瞬間に意図的に客を正面へ向き直させる」既存の
  演出処理や、検出スクリプト側のサンプリング間隔の粗さ（曲がり角をまたぐサンプルで
  誤検知する）で説明がつくものだった。また、このゲームの素材には「上向き（奥へ歩く）」
  の絵が存在せず、上方向への移動は代わりに「right」の絵を流用する仕様になっている
  （Phase 5時点から存在する既知の素材上の制約）。ユーザーが指摘した症状がこの
  代用そのものを指している可能性があり、これは新しい素材が無ければ解決できない
  既知の制約であるため、次回ユーザーへの報告時に「具体的にどの場面のどのキャラクターが
  正面を向いたままだったか」を確認したい。
  検証は、(1)通常速度・倍速それぞれでの移動方向とスプライトの向きの一致率の統計検証、
  (2)再生中に曜日タブ・前の日・次の日ボタンをランダムに連打し続ける「荒っぽい操作」
  耐性テスト（90秒×2パターン、いずれもコンソールエラー0件・持続的重なり0件・不自然な
  消失0件）、(3)修正前後のコードそれぞれでスクリーンショットを撮っての目視比較、
  (4)従来からの5パターン1週間フル再生の回帰確認（全パターンで週が正常完走、コンソール
  エラー0件、持続的な重なり最大3件、本当に不自然な消失0件）、を実施した。
  修正版`js/ui/scene.js`・`js/ui/field.js`をPCへ反映済み（サイズ一致確認済み）。
